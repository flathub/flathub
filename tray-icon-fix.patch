--- a/electroncash_gui/qt/__init__.py
+++ b/electroncash_gui/qt/__init__.py
@@ -522,10 +522,14 @@ class ElectrumGui:
         self.tray.setContextMenu(m)
 
     def tray_icon(self):
+        # Use themed icons for proper StatusNotifierItem/D-Bus support
+        # This allows system trays (KDE, GNOME) to find the icon via icon theme
         if self.dark_icon:
-            return QIcon(':icons/electron_dark_icon.svg')
+            icon = QIcon.fromTheme('org.electroncash.ElectronCash-dark')
+            return icon if not icon.isNull() else QIcon(':icons/electron_dark_icon.svg')
         else:
-            return QIcon(':icons/electron_light_icon.svg')
+            icon = QIcon.fromTheme('org.electroncash.ElectronCash-light')
+            return icon if not icon.isNull() else QIcon(':icons/electron_light_icon.svg')
 
     def toggle_tray_icon(self):
