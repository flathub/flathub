app-id: io.github.comicdeed.jots
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
base: io.elementary.BaseApp
base-version: 'circe-25.08'
command: io.github.comicdeed.jots

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --socket=session-bus
  - --own-name=io.github.comicdeed.jots
  - --filesystem=xdg-data/io.github.elly_code.jorts:ro
  - --filesystem=~/.var/app/io.github.elly_code.jorts:ro
  - --device=dri

cleanup:
  - /include
  - /lib/girepository-1.0
  - /lib/pkgconfig
  - /share/gir-1.0
  - /share/vala

cleanup-commands:
  - /app/cleanup-BaseApp.sh

modules:
  - name: jots
    buildsystem: meson
    config-opts:
      - --buildtype=release
      - -Dicon_variant=default
    sources:
      - type: archive
        url: https://github.com/comicdeed/jots/archive/refs/tags/1.0.0-beta.1.tar.gz
        sha256: e115f3d482cfbbc501a6914080f8b3c65abac1ebae838811575b8a77386b53cf
