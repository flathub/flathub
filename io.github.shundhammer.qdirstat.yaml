app-id: io.github.shundhammer.qdirstat
runtime: org.kde.Platform
runtime-version: 5.15-24.08
sdk: org.kde.Sdk
command: qdirstat
rename-desktop-file: qdirstat.desktop
rename-icon: qdirstat
finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --filesystem=host

modules:
  - name: QDirStat
    buildsystem: qmake
    config-opts:
      - INSTALL_PREFIX=/app
    sources:
      - type: git
        url: https://github.com/shundhammer/qdirstat.git
        tag: 1.9
        commit: 9dd849bc7011f4791319ae716f0482e4e4accdec
        x-checker-data:
          type: git
          tag-pattern: ^([\d.]+)$
      - type: patch
        path: appstream.patch
      - type: patch
        path: remove-unsandboxed-actions.patch
