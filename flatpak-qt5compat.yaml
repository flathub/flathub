## All the dependencies we need to build Qt6/5 compatibility libs from source.
name: qt5compat
build-commands: []
buildsystem: simple
modules:
  - name: libqt6core5compat6
    buildsystem: cmake-ninja
    sources:
      - type: git
        tag: v6.6.3
        url: https://github.com/qt/qt5compat
