id: com.beeper.Beeper
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: beepertexts
finish-args:
  - --device=dri
  - --filesystem=xdg-download
  - --filesystem=xdg-pictures:ro
  - --filesystem=xdg-videos:ro
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --socket=wayland
  - --talk-name=org.freedesktop.Notifications
modules:
  - shared-modules/libappindicator/libappindicator-gtk3-12.10.json
  - shared-modules/libsecret/libsecret.json
  - shared-modules/squashfs-tools/squashfs-tools.json
  - name: beepertexts
    buildsystem: simple
    sources:
      - type: script
        dest-filename: beepertexts
        commands:
          - zypak-wrapper /app/extra/beepertexts/beepertexts "$@"
      - type: extra-data
        only-arches:
          - x86_64
        url: https://beeper-desktop.download.beeper.com/builds/Beeper-4.2.808-x86_64.AppImage
        sha256: aa5e5690a5604222881e434a77cd39c457b3b2fa16fbc76a5f1e8ccdfb3171e3
        size: 236292417
        filename: Beeper.AppImage
        x-checker-data:
          type: rotating-url
          url: https://api.beeper.com/desktop/download/linux/x64/stable/com.automattic.beeper.desktop
      - type: file
        path: com.beeper.Beeper.metainfo.xml
      - type: file
        path: icons/com.beeper.Beeper.png
      - type: file
        path: com.beeper.Beeper.desktop
      - type: script
        dest_filename: apply_extra.sh
        commands:
          - chmod +x Beeper.AppImage
          - "offset=$(grep -aob -- 'hsqs' Beeper.AppImage | tail -n1 | cut -d: -f1)"
          - unsquashfs -quiet -no-progress -o $offset Beeper.AppImage
          - mv squashfs-root/* .
          - rm -rf Beeper.AppImage squashfs-root
    build-commands:
      - install -Dm755 apply_extra.sh ${FLATPAK_DEST}/bin/apply_extra
      - install -Dm644 ${FLATPAK_ID}.desktop -t ${FLATPAK_DEST}/share/applications
      - install -Dm644 ${FLATPAK_ID}.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo/
      - install -Dm644 ${FLATPAK_ID}.png -t ${FLATPAK_DEST}/share/icons/hicolor/512x512/apps
      - install -Dm755 beepertexts -t ${FLATPAK_DEST}/bin 
      - ln -s ${FLATPAK_DEST}/bin/beepertexts ${FLATPAK_DEST}/bin/beeper
