app-id: org.gnome.Raider
runtime: org.gnome.Platform
runtime-version: '1.0'
sdk: org.gnome.Sdk
command: raider
finish-args:
  - --share=ipc   
  - --socket=fallback-x11  
  - --socket=wayland   
  - --filesystem=home
  - --metadata=X-DConf=migrate-path=/org/gnome/raider/
cleanup:
  - /man
  - /share/man
modules:
  - name: raider
    buildsystem: meson
    sources:
      - type: archive
      url: https://github.com/ADBeveridge/raider/archive/refs/heads/main.zip
      sha256: 661b1482942cb6d765719faaf3d938212bf25acb2d89233e5607d63880d7656e

