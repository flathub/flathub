app-id: io.github.manisandro.gImageReader
runtime: org.gnome.Platform
runtime-version: "43"
sdk: org.gnome.Sdk
command: gimagereader-gtk
rename-desktop-file: gimagereader-gtk.desktop
rename-appdata-file: gimagereader-gtk.appdata.xml
rename-icon: gimagereader

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri

modules:
  - name: gimagereader
    buildsystem: cmake
    # no-make-install: true
    config-opts:
      - -DCMAKE_INSTALL_PREFIX='/app'
      - -DINTERFACE_TYPE=gtk
      - -DENABLE_VERSIONCHECK=0
    # post-install:
    #   - install -Dm755 output/x64/None/QtScrcpy -t "${FLATPAK_DEST}/qtscrcpy/"
    #   - install -Dm644 output/x64/None/scrcpy-server -t "${FLATPAK_DEST}/qtscrcpy/"
    #   - install -Dm644 output/x64/None/sndcpy.apk "${FLATPAK_DEST}/qtscrcpy/"
    #   - install -Dm755 output/x64/None/sndcpy.sh "${FLATPAK_DEST}/qtscrcpy/"
    #   - install -Dm644 config/config.ini -t "${FLATPAK_DEST}/etc/qtscrcpy/"
    #   - install -Dm644 backup/logo.png "${FLATPAK_DEST}/share/icons/hicolor/512x512/apps/${FLATPAK_ID}.png"
    #   - install -Dm644 io.github.manisandro.gImageReader.desktop "${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop"
    #   - install -Dm644 io.github.manisandro.gImageReader.metainfo.xml "${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml"
    #   - install -Dm755 qtscrcpy.sh "${FLATPAK_DEST}/bin/qtscrcpy"
    #   - |
    #     for res in 16 32 48 64 128 256 512; do
    #       convert +set date:create +set date:modify backup/logo.png -resize ${res}x${res} \
    #         -background transparent -gravity center -extent ${res}x${res} icon_${res}.png || exit 1
    #       install -Dm0644 icon_${res}.png ${FLATPAK_DEST}/share/icons/hicolor/${res}x${res}/apps/${FLATPAK_ID}.png || exit 1
    #     done
    sources:
      - type: git
        url: https://github.com/manisandro/gimagereader
        tag: v3.4.0
        commit: 75314538bc43cc651a19e00891a7170a6e228881
        x-checker-data:
          type: anitya
          project-id: 8889
          stable-only: true
          tag-template: v$version
    modules:
      - sane-backends.yml
      - podofo.yml
      - djvulibre.yml
      - tesseract.yml
      - shared-modules/intltool/intltool-0.51.json
      - gtkmm.yml
      - gtksourceviewmm.yml
      - poppler.yml
      - libxml++.yml
      - libzip.yml
