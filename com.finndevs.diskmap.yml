app-id: com.finndevs.diskmap
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: diskmap

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --filesystem=host:ro
  - --filesystem=home
  - --talk-name=org.freedesktop.FileManager1

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/diskmap/cargo

modules:
  - name: diskmap
    buildsystem: simple
    build-commands:
      - cargo --offline build --release -p dm-gtk
      - install -Dm755 target/release/diskmap /app/bin/diskmap
      - install -Dm644 packaging/com.finndevs.diskmap.desktop /app/share/applications/com.finndevs.diskmap.desktop
      - install -Dm644 packaging/com.finndevs.diskmap.svg /app/share/icons/hicolor/scalable/apps/com.finndevs.diskmap.svg
      - install -Dm644 packaging/com.finndevs.diskmap.metainfo.xml /app/share/metainfo/com.finndevs.diskmap.metainfo.xml
    sources:
      - type: git
        url: https://github.com/finn-devs/diskmap.git
        tag: v1.0.0
        commit: b6ffad86ae07fb1c5f7907e17a398f83a417f1a8
      # Regenerate with: python3 scripts/generate-cargo-sources.py
      - cargo-sources.json
