id: page.codeberg.jaerrib.RNGremlin
runtime: org.freedesktop.Platform
runtime-version: '25.08'
base: org.godotengine.godot.BaseApp
base-version: '4.7'
sdk: org.freedesktop.Sdk
command: godot-runner

finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --device=dri

modules:
  - name: RNGremlin
    buildsystem: simple

    sources:
      - type: git
        url: https://codeberg.org/jaerrib/RNGremlin.git
        tag: 2.0.0
        commit: f7086861f0abff530b6cdabdff3c7cd2cbe99065

      - type: file
        url: https://codeberg.org/jaerrib/RNGremlin/releases/download/2.0.0/RNGremlin-2.0.0.pck
        dest-filename: RNGremlin.pck
        sha256: 6b58bd49c43378351080e2d7db4b46c1fa9d4fe1d2741a8644467e28e9709532

    build-commands:
      - install -Dm644 RNGremlin.pck ${FLATPAK_DEST}/bin/godot-runner.pck
      - install -Dm644 ${FLATPAK_ID}.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm644 ${FLATPAK_ID}.xml ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm644 ${FLATPAK_ID}.png ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
