app-id: io.github.atharva2012tiwari.timeler
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: timeler

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio

modules:
  - name: timeler
    buildsystem: simple
    build-commands:
      # Binary and Flutter data bundle
      - install -D -m 0755 timeler ${FLATPAK_DEST}/bin/timeler
      - cp -r data/ ${FLATPAK_DEST}/bin/
      - cp -r lib/ ${FLATPAK_DEST}/bin/
      # Desktop entry and metadata
      - install -D -m 0644 io.github.atharva2012tiwari.timeler.desktop ${FLATPAK_DEST}/share/applications/io.github.atharva2012tiwari.timeler.desktop
      - install -D -m 0644 io.github.atharva2012tiwari.timeler.metainfo.xml ${FLATPAK_DEST}/share/metainfo/io.github.atharva2012tiwari.timeler.metainfo.xml
      # Scalable Vector Icon & PNG for GNOME
      - install -D -m 0644 io.github.atharva2012tiwari.timeler.svg ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/io.github.atharva2012tiwari.timeler.svg
      - install -D -m 0644 io.github.atharva2012tiwari.timeler.png ${FLATPAK_DEST}/share/icons/hicolor/512x512/apps/io.github.atharva2012tiwari.timeler.png
    sources:
      - type: archive
        url: https://github.com/atharva2012tiwari/timeler/releases/download/v1.0.0/timeler-linux-x64.tar.gz
        sha256: 56c17f8ca810d2aed62030583bab17babfdfef7c2bb58c392e7432a39279dc5c
      - type: file
        path: io.github.atharva2012tiwari.timeler.desktop
      - type: file
        path: io.github.atharva2012tiwari.timeler.metainfo.xml
      - type: file
        path: io.github.atharva2012tiwari.timeler.svg
      - type: file
        path: io.github.atharva2012tiwari.timeler.png
