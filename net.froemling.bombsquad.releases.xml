<?xml version='1.0' encoding='utf-8'?>
<releases>
    <release version="1.7.62" date="2026-05-04" urgency="low" type="stable">
        <description>
            <ul>
                <li>Added initial support for signing in with a Discord account as a first-class
  V2 login type, via Discord's Social SDK. Discord support is included in Mac,
  Windows, and Linux test-builds for now, but will expand to Android and Mac App
  Store builds soon. We can also start taking advantage of other features of the
  Discord SDK such as rich presence or voice chat once any initial bugs are
  ironed out. (Thanks Loup-Garou911XD for the proof-of-concept implementation!).</li>
                <li>Regional server connections have been overhauled and now use websockets on
  standard http/https ports. This should help reduce blocking and should
  establish connections much faster. Please holler if you run into connectivity
  problems as this is a significant code overhaul.</li>
                <li>Added `tests/test_restapi` which can be useful as reference for the
  ballistica.net REST api. Run `make test-restapi` to run all REST api tests
  (you just need to supply an API Key).</li>
                <li>Fixed Spaz being able to punch very shortly after executing a grab, but
  before actually grabbing. This fixes the long standing punch grab infinite
  exploit that allows for effortless kills on players. (Thanks TheMikirog!).</li>
                <li>Added protocol 37 which allows spaz behavior_version to be set to 2 which
  enables the above behavior. Default protocol version is still 33.</li>
                <li>Spaz turbo-filter code has been moved from Spaz to PlayerSpaz and button press
  logic has been cleaned up (Thanks vinnytherabbit!)</li>
                <li>Added `BA_LOG_LEVELS` env var to override log levels at launch (e.g.
  `BA_LOG_LEVELS='ba.net=DEBUG,ba.connectivity=DEBUG'`).</li>
                <li>The engine's networking now honors the standard `HTTPS_PROXY`
  environment variable, so the game works behind corporate /
  sandboxed proxies without manual configuration.</li>
                <li>Added `test_game_run` pcommand for automated game testing.</li>
                <li>Players finally have all their unlocked characters accurately available when
  connecting to servers with v2-auth enabled.</li>
                <li>Servers with v2-auth enabled can now see verified classic_purchases for
  connected clients (without v2-auth this will show up as None).</li>
                <li>Workspace uploads are no longer are limited to 10mb (applies to web UI, REST,
  and bacloud). Note that syncing workspaces to the client currently does not
  support workspaces with such large files (that will mainly be for assets).</li>
                <li>Added scale transition option for widget and wired up doc-ui to use that
  instead of in-left transitions.</li>
                <li>Very early logs should now be displayed consistently and with accurate timing
  info (good for debugging bootstrapping/lifecycle issues).</li>
                <li>The 'Use insecure connections' checkbox is now an always/auto/never popup with
  'auto' being the default. In auto mode the app may opt to use insecure
  connections in environments that are known to muck with or block secure ones.
  Choose always or never if you don't want to give it that choice.</li>
                <li>Added support for platforms to provide a 'network-available' state. This
  allows us to skip trying to talk to cloud stuff when there's no network
  connection or when we're in airplane mode or whatever which should save some
  battery, and also it lets us reconnect *exactly* when the network comes back
  online.</li>
                </ul>
        </description>
        <url>https://github.com/efroemling/ballistica/releases/tag/v1.7.62</url>
        <artifacts>
            <artifact type="source">
                <location>https://github.com/efroemling/ballistica/archive/refs/tags/v1.7.62.tar.gz</location>
                </artifact>
            <artifact type="source" platform="x86_64-linux-gnu">
                <location>https://github.com/efroemling/ballistica/releases/download/v1.7.62/bombsquad_build_env.tar</location>
                <checksum type="sha256">cb79efcfbed3ccabb71ce9548868b8436c5cf9ae5d9a68ee8f5208919341ceaf</checksum>
                </artifact>
        </artifacts>
    </release>
    <release version="1.7.59" date="2026-01-13" urgency="low" type="stable">
        <description>
            <p>This stable release fixes bugs.</p>
            </description>
    </release>
</releases>
