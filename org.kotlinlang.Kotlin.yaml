app-id: org.kotlinlang.Kotlin
sdk: org.freedesktop.Sdk
runtime: org.freedesktop.Sdk
runtime-version: '22.08'
command: run.sh

sdk-extensions:
  - org.freedesktop.Sdk.Extension.openjdk17

build-options:
  prepend-path: /app/jre/bin

finish-args:
  - --env=PATH=/app/jre/bin:/usr/bin:/app/bin
  - --filesystem=host

modules:
    # This is a java program, ofc we need this
  - name: openjdk
    buildsystem: simple
    build-commands:
      - /usr/lib/sdk/openjdk17/install.sh

  - name: kotlinc
    buildsystem: simple
    build-commands:
      - install -D ./bin/* -t /app/bin/
      - install -D ./lib/* -t /app/lib/
      - install -D ${FLATPAK_ID}.metainfo.xml -t /app/share/metainfo/
    sources:
      - type: archive
        url: https://github.com/JetBrains/kotlin/releases/download/v1.8.22/kotlin-compiler-1.8.22.zip
        tag: v1.7.10
        sha256: 91f50fe25c9edfb1e79ae1fe2ede85fa4728f7f4b0587644a4eee40252cdfaa6
        x-checker-data:
          type: json
          url: https://api.github.com/repos/JetBrains/kotlin/releases/latest
          version-query: .tag_name | sub("^v"; "")
          url-query: .assets[] | select(.name=="kotlin-compiler-" + $version + ".zip")
            | .browser_download_url
      - type: file
        path: org.kotlinlang.Kotlin.metainfo.xml

  - name: run
    buildsystem: simple
    build-commands:
      - install -D run.sh -t /app/bin/
    sources:
      - type: file
        path: run.sh
