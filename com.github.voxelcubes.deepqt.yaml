app-id: com.github.voxelcubes.deepqt
runtime: org.kde.Platform
runtime-version: '5.15-22.08'
sdk: org.kde.Sdk
command: deepqt
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --filesystem=xdg-config
  - --filesystem=xdg-cache
  - --filesystem=home
  - --share=network
modules:
  - pypi-dependencies.yaml

  - name: deepqt
    buildsystem: simple
    build-commands:
      - 'pip3 install --no-index --find-links="file://${PWD}" --prefix=${FLATPAK_DEST} deepqt'
    sources:
      - type: file
        path: DeepQt-flatpak.desktop
      - type: file
        path: deepqt.png
    post-install:
      - install -Dm644 DeepQt-flatpak.desktop /app/share/applications/com.github.voxelcubes.deepqt.desktop
      - install -Dm644 deepqt.png /app/share/icons/hicolor/256x256/apps/com.github.voxelcubes.deepqt.png
