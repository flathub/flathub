id: com.obsproject.Studio.Plugin.RetroEffects
runtime: com.obsproject.Studio
runtime-version: stable
sdk: org.kde.Sdk//6.6
build-extension: true
separate-locales: false
build-options:
  prefix: /app/plugins/RetroEffects
modules:
  - name: retro-effects
    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DBUILD_OUT_OF_TREE=On
    post-install:
      - install -Dm644 --target-directory=${FLATPAK_DEST}/share/metainfo ${FLATPAK_ID}.metainfo.xml
    sources:
      - type: git
        url: https://github.com/FiniteSingularity/obs-retro-effects.git
        tag: v1.0.3
        commit: 9bce6971c038eb60bbaa414477080cbf7c630320
        x-checker-data:
          type: git
          is-main-source: true
          tag-pattern: ^v([\d.]+)$
      - type: file
        path: com.obsproject.Studio.Plugin.RetroEffects.metainfo.xml