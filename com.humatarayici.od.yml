app-id: com.humatarayici.od
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
base: org.mozilla.firefox.BaseApp
base-version: '23.08'
command: huma-launcher.sh
finish-args:
  - --share=ipc
  - --share=network
  - --socket=pulseaudio
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pcsc
  - --socket=cups
  - --persist=.huma
  - --filesystem=xdg-download:rw
  - --device=all
  - --talk-name=org.freedesktop.FileManager1
  - --own-name=org.mozilla.huma.*
  - --own-name=org.mpris.MediaPlayer2.firefox.*
  - --system-talk-name=org.freedesktop.NetworkManager
  - --talk-name=org.a11y.Bus
modules:
  - name: huma_browser
    buildsystem: simple
    build-commands:
      - mv huma /app/

      - install -Dm0755 metadata/huma-launcher.sh ${FLATPAK_DEST}/bin/huma-launcher.sh
      - install -Dm0644 metadata/policies.json ${FLATPAK_DEST}/bin/distribution/policies.json
      - install -Dm0644 metadata/icons/com.humatarayici.od.png ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
      - install -Dm0644 metadata/com.humatarayici.od.metainfo.xml ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm0644 metadata/com.humatarayici.od.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop

    sources:
      - type: archive
        url: https://github.com/VastSea0/Huma/releases/download/0a16/flatpak.zip
        sha256: 69695f6fbf957c567470c9d796d3772aec822ea8c6c501c86d5ccac763e5ed48
        strip-components: 0

      - type: archive
        url: https://github.com/vastsea0/flatpak/releases/latest/download/archive.zip
        sha256: 1abffdbb329ba3dec10caec2e42391cd0084d27965c360e299fc1e8e8900d150
        strip-components: 0
        dest: metadata