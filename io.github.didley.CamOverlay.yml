id: io.github.didley.CamOverlay
runtime: org.gnome.Platform
runtime-version: "50"
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: camoverlay
finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --filesystem=xdg-run/pipewire-0:ro
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/camoverlay/cargo-home
    RUST_BACKTRACE: "1"
modules:
  - name: camoverlay
    buildsystem: meson
    config-opts:
      - -Dbuildtype=release
    sources:
      - type: git
        url: https://github.com/didley/camoverlay.git
        tag: v1.0.0
        commit: 6a3da1c4d16326d568fcfcdaeebc9a3a3518e14e
      - cargo-sources.json
      - type: inline
        contents: |
          [source.crates-io]
          replace-with = "vendored-sources"

          [source.vendored-sources]
          directory = "cargo/vendor"
        dest: .cargo
        dest-filename: config.toml
