id: com.obsproject.Studio.Plugin.MuteFilter
branch: stable
runtime: com.obsproject.Studio//stable
sdk: org.kde.Sdk//6.8
build-extension: true
separate-locales: false
build-options:
  prefix: /app/plugins/MuteFilter
  strip: true

modules:
  - name: mute-filter

    sources:
      - type: git
        url: https://github.com/norihiro/obs-mute-filter.git
        tag: 0.3.0
      - type: patch
        path: project-name.patch
      - type: file
        path: com.obsproject.Studio.Plugin.MuteFilter.metainfo.xml

    buildsystem: cmake-ninja
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DLINUX_PORTABLE=OFF
      - -DLINUX_RPATH=OFF

    post-install:
      - install -Dm0644 --target-directory=${FLATPAK_DEST}/share/metainfo ${FLATPAK_ID}.metainfo.xml
      - install -Dm0644 LICENSE -t ${FLATPAK_DEST}/share/licenses/$FLATPAK_ID/mute-filter
