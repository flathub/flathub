app-id: io.github.giantpinkrobots.bootqt
runtime: org.kde.Platform
runtime-version: '5.15-21.08'
sdk: org.kde.Sdk
base: com.riverbankcomputing.PyQt.BaseApp
base-version: 5.15-21.08

command: bootqt.sh

finish-args:
  - --device=all
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --filesystem=host
  - --talk-name=org.freedesktop.Flatpak

cleanup-commands:
  - /app/cleanup-BaseApp.sh
build-options:
  env:
    - BASEAPP_REMOVE_WEBENGINE=1
  
modules:
  - name: bootqt
    buildsystem: simple
    build-commands:
      - install -D -m755 flatpak/bootqt.sh /app/bin/bootqt.sh
      - install -D -m755 flatpak/bootqt.py -t /app/lib/bootqt
      - install -D -m755 flatpak/io.github.giantpinkrobots.bootqt.png -t /app/share/icons/hicolor/128x128/apps
      - install -D -m755 flatpak/io.github.giantpinkrobots.bootqt.desktop -t /app/share/applications
      - install -D -m755 io.github.giantpinkrobots.bootqt.metainfo.xml /app/share/appdata/
    
    sources:
      - type: git
        url: https://github.com/giantpinkrobots/bootqt.git
        commit: 4b125312beee398a806ccb47a7524703e0ef84d0
      - type: file
        path: io.github.giantpinkrobots.bootqt.metainfo.xml
