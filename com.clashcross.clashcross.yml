# yaml-language-server: $schema=https://raw.githubusercontent.com/flatpak/flatpak-builder/main/data/flatpak-manifest.schema.json
app-id: com.clashcross.clashcross
runtime: org.freedesktop.Platform
runtime-version: "22.08"
sdk: org.freedesktop.Sdk
command: clashcross
separate-locales: false

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=pulseaudio
  - --share=network
  - --socket=wayland
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.kde.StatusNotifierWatcher
modules:
  - shared-modules/libappindicator/libappindicator-gtk3-introspection-12.10.json
  # ClashCross
  - name: ClashCross
    buildsystem: simple
    only-arches:
      - x86_64
    build-commands:
      - mkdir -p clashcross
      - tar -xf clashcross-linux-x86_64.tar.xz -C clashcross


      # Copy the portable app to the Flatpak-based location.
      - cp -r clashcross /app/
      - chmod +x /app/clashcross/clashcross
      - mkdir -p /app/bin
      - ln -s /app/clashcross/clashcross /app/bin/clashcross

      # Install the icon.
      - mkdir -p /app/share/icons/hicolor/scalable/apps
      #cp -r assets/icons/$projectId.svg $iconDir/
      - cp -r com.clashcross.clashcross.png /app/share/icons/hicolor/scalable/apps/

      # Install the desktop file.
      - mkdir -p /app/share/applications
      - cp -r com.clashcross.clashcross.desktop /app/share/applications/

      # Install the AppStream metadata file.
      - mkdir -p /app/share/metainfo
      - cp -r metainfo.xml /app/share/metainfo/

    sources:
      - type: file
        url: https://github.com/shipinbaoku/clashcross/releases/download/v1.0.3/clashcross-linux-x86_64.tar.xz
        sha256: 26f462bb9475c41d04c69437073767394ee85663b2bdc98cf020483c378ec551
      - type: dir
        path: ./files