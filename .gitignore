# flatpak-builder build artifacts
build-dir/
repo/
.flatpak-builder/

# pre-built frontend dist tarballs (downloaded at build time from GitHub Releases)
authenticator-dist.tar.gz
proton-authenticator-dist-*.tar.gz

# CI/local working directories
_webclient/
_tools/
