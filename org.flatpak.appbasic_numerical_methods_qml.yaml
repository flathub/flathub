    # Example Flatpak manifest for a simple Qt application
    app-id: org.flatpak.appbasic_numerical_methods_qml
    runtime: org.kde.Platform
    separate-locales: false
    runtime-version: "6.9" # Or the appropriate Qt version
    sdk: org.kde.Sdk
    command: appbasic_numerical_methods_qml
    finish-args:
        - --share=ipc
        - --socket=fallback-x11
        - --socket=wayland
        - --device=dri
        - --share=network
        - --filesystem=host
    modules:
      - name: appbasic_numerical_methods_qml
        buildsystem: cmake # Or cmake, meson, etc. depending on your build system
        sources:
          - type: dir
            path: /home/hafmed/Documents/24-25/basic_numerical_methods_qml/ # Path to your application source code
        post-install:
        - install -Dm644 org.flatpak.appbasic_numerical_methods_qml.metainfo.xml /app/share/metainfo/org.flatpak.appbasic_numerical_methods_qml.metainfo.xml
        - install -Dm644 org.flatpak.appbasic_numerical_methods_qml.desktop /app/share/applications/org.flatpak.appbasic_numerical_methods_qml.desktop
        #- install -Dm644 my-app.appdata.xml /app/share/metainfo/my-app.appdata.xml
        - mkdir -p /app/share/icons/hicolor/scalable/apps
        - install -Dm644 appbasic_numerical_methods_qml_256.png /app/share/icons/hicolor/scalable/apps/appbasic_numerical_methods_qml_256.png
        #.svg



