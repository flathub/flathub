app-id: com.ktechpit.red
base: io.qt.qtwebengine.BaseApp
base-version: '6.11'
runtime: org.kde.Platform
runtime-version: '6.11'
sdk: org.kde.Sdk
command: red
finish-args:
  - --device=all
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio

  - --own-name=org.mpris.MediaPlayer2.red
  - --talk-name=org.kde.StatusNotifierWatcher

  - --filesystem=/run/udev:ro
  - --filesystem=xdg-download

cleanup:
  - /bin/mksquashfs
  - /bin/unsquashfs
  - /bin/sqfscat
  - /bin/sqfstar

cleanup-commands:
  - /app/cleanup-BaseApp.sh

modules:
  - shared-modules/squashfs-tools/squashfs-tools.json

  - name: red
    buildsystem: simple
    build-commands:
      - unsquashfs -no-progress *.snap
      - install -Dm755 squashfs-root/usr/bin/red ${FLATPAK_DEST}/bin/red
      - install -Dm755 squashfs-root/usr/bin/deno ${FLATPAK_DEST}/bin/deno
      - cp -r squashfs-root/usr/share/icons ${FLATPAK_DEST}/share/
      - install -Dm644 squashfs-root/usr/share/applications/com.ktechpit.red.desktop ${FLATPAK_DEST}/share/applications/com.ktechpit.red.desktop
      - desktop-file-edit --set-icon=${FLATPAK_ID} ${FLATPAK_DEST}/share/applications/com.ktechpit.red.desktop
      - install -Dm644 squashfs-root/usr/share/metainfo/com.ktechpit.red.metainfo.xml ${FLATPAK_DEST}/share/metainfo/com.ktechpit.red.metainfo.xml
    sources:
      # =========================
      # x86_64 (amd64) - stable
      # =========================
      - type: file
        url: https://api.snapcraft.io/api/v1/snaps/download/46Yk5UkQgwdukH9Fw6IeJOkGhyELYKtQ_57.snap
        sha256: 5af659121f38700ab7aad62993a872d695995f38757d2d8165795b0f8244e976
        only-arches:
          - x86_64

      # =========================
      # aarch64 (arm64) - stable
      # =========================
      - type: file
        url: https://api.snapcraft.io/api/v1/snaps/download/46Yk5UkQgwdukH9Fw6IeJOkGhyELYKtQ_58.snap
        sha256: d5230f99de0563c87d1fdb7f193a5fe9633877e22aa84232adbd0b2414dd93d7
        only-arches:
          - aarch64
