# https://github.com/flathub/no.mifi.losslesscut/blob/0be4bb6d40da5ba9d9fef1831d14f0b8e03e9bf3/ffmpeg.yaml
name: ffmpeg
config-opts:
  - --enable-shared
  - --disable-static
  - --disable-doc
  - --disable-manpages
  # - --disable-stripping
  - --disable-ffplay
  - --enable-ffprobe
  - --disable-lzma
  - --enable-runtime-cpudetect
  - --disable-xlib
  - --disable-libxcb-shm
  - --disable-libxcb-xfixes
  - --disable-libxcb-shape
  - --disable-outdevs
  - --disable-indevs
  # - --enable-indev=lavfi

  - --disable-securetransport
  # - --enable-runtime-cpudetect

  - --enable-gpl
  - --enable-version3
  - --enable-optimizations
  - --enable-pthreads
  #- --enable-gnutls
  # - --enable-libxcb-xfixes
  # - --enable-opengl
  #- --enable-sdl2
  # - --enable-vaapi
  # - --enable-vdpau
  #- --enable-zlib
  #- --enable-bzlib
  #- --enable-lzma

  # - --enable-fontconfig
  # - --enable-libfontconfig
  # - --enable-libfreetype

  # copied from another manifest
  - --enable-avcodec
  - --enable-avfilter
  - --enable-filters
  # - --enable-avdevice
  #- --enable-libxml2
  - --enable-swscale

  # - --enable-libxcb

  - --enable-protocol=file

#post-install:
    # This is what losslesscut tried to execute:
    #- /app/bin/ffmpeg -hide_banner -f lavfi -i nullsrc=s=256x256:d=1 -f null -

sources:
  - type: archive
    url: https://ffmpeg.org/releases/ffmpeg-8.1.2.tar.xz
    sha512: b3adc16fe426217bb607da01c4137cee0a9788fc08e077874336db185d2b7287746a7dc94cf0181ea92cd8afcdb06094ee9456e2986112354f84538cb9a5ed0b
    x-checker-data:
      type: anitya
      project-id: 5405
      url-template: https://ffmpeg.org/releases/ffmpeg-$version.tar.xz
cleanup:
  - /share/ffmpeg/examples
