# Manifest for https://github.com/TheAppgineer/flatpak-flutter
# podman run --rm -v $(pwd):/usr/src/flatpak --userns=keep-id docker.io/theappgineer/flatpak-flutter:latest --app-pubspec example flatpak-flutter.yml
id: org.unifiedpush.Example
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: unifiedpush_example
finish-args:
  # X11 fallback
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  # To send the test notification
  - --share=network
  # UnifiedPush
  - --talk-name=org.unifiedpush.Distributor.*
  # We don't need to explicitly own org.unifiedpush.Example, as our id
  # is automatically allowed, if we were using a different service name:
  #- --own-name=org.unifiedpush.Example
  #
  # Used to show notifications
  - --talk-name=org.freedesktop.Notifications
modules:
  - name: Example
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        PUB_CACHE: /run/build/Example/.pub-cache
    build-commands:
      - cd example && flutter build linux --release --no-pub
      - install -Dm0755 example/${BUNDLE_PATH}/unifiedpush_example -t ${FLATPAK_DEST}/bin
      - cp -r example/${BUNDLE_PATH}/lib ${FLATPAK_DEST}/bin/lib
      - cp -r example/${BUNDLE_PATH}/data ${FLATPAK_DEST}/bin/data
    post-install:
      - install -Dm0644 example/flatpak/assets/LICENSE -t ${FLATPAK_DEST}/share/licenses/$FLATPAK_ID/Example
      - install -Dm0644 example/flatpak/assets/org.unifiedpush.Example.png -t ${FLATPAK_DEST}/share/icons/hicolor/512x512/apps
      - install -Dm0644 example/flatpak/assets/org.unifiedpush.Example.service -t ${FLATPAK_DEST}/share/dbus-1/services
      - install -Dm0644 example/flatpak/assets/org.unifiedpush.Example.desktop -t ${FLATPAK_DEST}/share/applications
      - install -Dm0644 example/flatpak/assets/org.unifiedpush.Example.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo
    sources:
      - type: git
        url: https://codeberg.org/UnifiedPush/flutter-connector
        commit: 70af6ffe461534b320f4667af55eb09ebb004267
        #tag: 6.2.0
      - type: git
        url: https://github.com/flutter/flutter
        tag: 3.35.4
        dest: flutter
