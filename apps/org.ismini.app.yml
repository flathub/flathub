app-id: org.ismini.app
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: ismini
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=all
  - --filesystem=host
  - --talk-name=org.freedesktop.ScreenCast
  - --talk-name=org.freedesktop.portal.ScreenCast
  - --talk-name=org.freedesktop.portal.Background

modules:
  # 1. Node.js 22 LTS (bundled — no system dependency needed)
  - name: nodejs
    buildsystem: simple
    build-commands: []
    sources:
      - type: archive
        url: https://nodejs.org/dist/v22.14.0/node-v22.14.0-linux-x64.tar.xz
        sha256: 69b09dba5c8dcb05c4e4273a4340db1005abeafe3927efda2bc5b249e80437ec
        only-architectures: [x86_64]
    # For flatpak-builder 1.4.8: use 'build-commands' to install into /app
    build-options:
      no-pkgconfig-path: true
    post-install:
      - mkdir -p /app/bin /app/lib /app/include
      - cp bin/node /app/bin/
      - cp bin/npx /app/bin/
      - cp bin/corepack /app/bin/
      - cp -r lib/* /app/lib/
      - cp -r include/* /app/include/
      - chmod +x /app/bin/node /app/bin/npx /app/bin/corepack

  # 2. Ismini application
  - name: ismini
    buildsystem: simple
    build-commands:
      - install -Dm755 ismini $DESTDIR/app/bin/ismini
      - install -Dm644 agent.js $DESTDIR/app/lib/ismini/agent.js
      - install -Dm644 web.js $DESTDIR/app/lib/ismini/web.js
      - install -Dm644 config.json $DESTDIR/app/lib/ismini/config.json
      - install -Dm644 ismini.png $DESTDIR/app/share/icons/hicolor/256x256/apps/org.ismini.app.png
      - install -Dm644 ismini.desktop $DESTDIR/app/share/applications/org.ismini.app.desktop
      - install -Dm644 web/index.html $DESTDIR/app/lib/ismini/web/index.html
      - install -Dm644 web/favicon-256.png $DESTDIR/app/lib/ismini/web/favicon-256.png
      - install -Dm644 web/marble.jpeg $DESTDIR/app/lib/ismini/web/marble.jpeg
      - install -Dm644 web/stars.gif $DESTDIR/app/lib/ismini/web/stars.gif
      - install -Dm644 web/bg.jpg $DESTDIR/app/lib/ismini/web/bg.jpg
      - cp -r web/fonts $DESTDIR/app/lib/ismini/web/fonts
      - cp -r tools $DESTDIR/app/lib/ismini/tools
      - install -Dm644 appstream/org.ismini.app.appdata.xml $DESTDIR/share/metainfo/org.ismini.app.appdata.xml
    sources:
      - type: git
        url: https://github.com/tasosdelotas/ismini.git
        branch: main
