app-id: org.getzola.zola
runtime: org.freedesktop.Platform
runtime-version: '20.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: zola

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
  env:
    CARGO_HOME: /run/build/zola/cargo

finish-args:
  - --share=network
  - --filesystem=host

modules:
  - name: zola
    buildsystem: simple
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      - cargo --offline build --release --verbose
      - install -Dm755 ./target/release/zola -t /app/bin/
      - install -D ./zola.appdata.xml $FLATPAK_DEST/share/metainfo/${FLATPAK_ID}.metainfo.xml
    sources:
      - type: git
        url: https://github.com/getzola/zola.git
        tag: v0.14.0
        commit: b2354c6cd8c44c99ab4978702915bdf9eddfa6f9
        disable-submodules: true
      - type: file
        path: ./zola.appdata.xml
      - cargo-sources.json
