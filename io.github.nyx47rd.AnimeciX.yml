app-id: io.github.nyx47rd.AnimeciX
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: animecix
separate-locales: false
finish-args:
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio
  - --talk-name=org.freedesktop.Flatpak
  - --filesystem=~/.local/share/singbox
cleanup:
  - /include
  - /lib/pkgconfig
  - '*.la'
modules:
  - name: ffmpeg
    buildsystem: simple
    build-commands:
      - ./configure --prefix=/app --enable-shared --disable-static --disable-doc --disable-programs --disable-avdevice --disable-postproc --disable-sdl2 --disable-vulkan
      - make -j${FLATPAK_BUILDER_N_JOBS}
      - make install
    sources:
      - type: git
        url: https://github.com/FFmpeg/FFmpeg.git
        tag: n7.1.1
        commit: db69d06eeeab4f46da15030a80d539efb4503ca8

  - name: libass
    buildsystem: autotools
    config-opts:
      - --disable-static
    sources:
      - type: archive
        url: https://github.com/libass/libass/releases/download/0.17.3/libass-0.17.3.tar.xz
        sha256: eae425da50f0015c21f7b3a9c7262a910f0218af469e22e2931462fed3c50959

  - name: mpv
    buildsystem: meson
    config-opts:
      - -Dcplayer=true
      - -Dlibmpv=false
      - -Dlua=disabled
      - -Djavascript=disabled
      - -Dlibarchive=disabled
      - -Dlibbluray=disabled
      - -Ddvdnav=disabled
      - -Dcdda=disabled
      - -Ddvbin=disabled
      - -Drubberband=disabled
      - -Dlibplacebo=disabled
      - -Dvulkan=disabled
      - -Dzimg=disabled
      - -Duchardet=disabled
      - -Dvapoursynth=disabled
      - -Dalsa=disabled
      - -Djack=disabled
      - -Dopenal=disabled
      - -Dsdl2=disabled
      - -Dpipewire-jack=disabled
      - -Dmanpage-build=disabled
      - -Dhtml-build=disabled
      - -Dpdf-build=disabled
    sources:
      - type: git
        url: https://github.com/mpv-player/mpv.git
        tag: v0.39.0
        commit: a0fba7be57f3822d967b04f0f6b6d6341e7516e7

  - name: animecix
    buildsystem: simple
    build-options:
      env:
        CARGO_HOME: /run/build/animecix/cargo
    build-commands:
      - cargo build --release --offline --locked
      - install -Dm755 target/release/animecix -t /app/bin
      - install -Dm644 packaging/flatpak/io.github.nyx47rd.AnimeciX.desktop -t /app/share/applications/
      - install -Dm644 packaging/flatpak/io.github.nyx47rd.AnimeciX.metainfo.xml -t /app/share/metainfo/
      - install -Dm644 assets/icon-64.png /app/share/icons/hicolor/64x64/apps/io.github.nyx47rd.AnimeciX.png
      - install -Dm644 assets/icon-128.png /app/share/icons/hicolor/128x128/apps/io.github.nyx47rd.AnimeciX.png
      - install -Dm644 assets/icon-256.png /app/share/icons/hicolor/256x256/apps/io.github.nyx47rd.AnimeciX.png
      - install -Dm644 assets/icon-512.png /app/share/icons/hicolor/512x512/apps/io.github.nyx47rd.AnimeciX.png
      - mkdir -p /app/share/animecix/assets
      - cp -r assets/upscale /app/share/animecix/assets/
      - chmod -R a-w /app/share/animecix/assets/upscale
    sources:
      - type: git
        url: https://github.com/nyx47rd/animecix-app.git
        tag: v2.9.3
        commit: 416626e0786edcbbc091bd72cbe0907fb072002b
      - cargo-sources.json
