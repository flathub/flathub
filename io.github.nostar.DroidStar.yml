app-id: io.github.nostar.DroidStar
runtime: org.kde.Platform
runtime-version: 5.15-22.08
sdk: org.kde.Sdk
command: droidstar
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --socket=pulseaudio
  - --device=dri
  - --filesystem=xdg-download
modules:
  - name: imbe_vocoder
    buildsystem: simple
    build-commands:
      - make
      - install -Dm644 libimbe_vocoder.a /app/lib/libimbe_vocoder.a
      - mkdir -p /app/include
      - install imbe_vocoder_api.h  /app/include/imbe_vocoder_api.h
    sources:
      - type: git
        url: https://github.com/nostar/imbe_vocoder.git
        branch: master
  - name: droidstar
    buildsystem: simple
    build-commands:
      - qmake LIBS+=-L/app/lib DroidStar.pro
      - make
    post-install:
      - install -Dm644 images/droidstar.png /app/share/icons/hicolor/128x128/apps/io.github.nostar.DroidStar.png
      - install -Dm644 com.github.nostar.DroidStar.desktop /app/share/applications/io.github.nostar.DroidStar.desktop
    sources:
      - type: git
        url: https://github.com/nostar/DroidStar.git
        branch: main

