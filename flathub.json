{
  "exceptions": {
    "finish-args-host-filesystem-access": "Moraine is a backup tool: it must read arbitrary user-selected source paths and write snapshots to the user-chosen destination (including external/mounted media)."
  }
}
