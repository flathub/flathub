{
    "only-arches": ["x86_64"],
    "x-flathub-comment": "x86_64 only: the game is tested and supported on x86_64; the compiled deps (tcod/_libtcod, numpy, miniaudio) build from source against the runtime's x86_64 SDL3"
}
