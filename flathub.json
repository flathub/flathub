{
  "finish-args-home-filesystem-access": "CsoundQt is an IDE for audio programming. Users store Csound projects and sample libraries anywhere in their home directory, and the Csound engine must resolve relative paths to audio assets referenced in orchestra files.",
  "skip-arches": ["aarch64"]
}
