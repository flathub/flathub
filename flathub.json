{
  "flathub-json-version": 1,
  "linter-exceptions": {
    "finish-args-has-socket-ssh-auth": "SSH agent access required for SSH key authentication",
    "finish-args-ssh-filesystem-access": "Read-only access to ~/.ssh required for SSH keys and config"
  }
}
