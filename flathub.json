{
  "$comment": "GeForce NOW itself is x86_64-only on Linux, so an aarch64 build would be a launcher with nothing to launch. This belongs here rather than in the manifest: only-arches is a Flathub build-config key, and flatpak-builder rejects it at manifest top level.",
  "only-arches": ["x86_64"]
}
