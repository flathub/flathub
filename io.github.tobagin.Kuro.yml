app-id: io.github.tobagin.Kuro
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: kuro
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: kuro
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tobagin/Kuro.git
        tag: 1.0.0
        commit: d91c84ef6f26d7c736e47521a5807359d7cfe62a
