app-id: io.github.tobagin.Kuro
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: kuro
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland

modules:
  - name: kuro
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tobagin/Kuro.git
        tag: 1.0.0
