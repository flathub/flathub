# Generated by flatpak-flutter v0.14.1 from flatpak-flutter.yaml, do not edit
# Visit the project at https://github.com/TheAppgineer/flatpak-flutter
id: xyz.nokarin.aqloss
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: Aqloss
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --socket=pulseaudio
modules:
  - name: Aqloss
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin:/var/lib/rustup/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        FLATPAK: "true"
        PUB_CACHE: /run/build/Aqloss/.pub-cache
        CARGO_HOME: /run/build/Aqloss/cargo
        RUSTUP_HOME: /var/lib/rustup
    build-commands:
      - setup-flutter.sh
      - flutter build linux --release --no-pub --dart-define=FLATPAK=true
      - install -Dm755 $BUNDLE_PATH/share/xyz.nokarin.aqloss/Aqloss /app/share/xyz.nokarin.aqloss/Aqloss
      - cp -r $BUNDLE_PATH/share/xyz.nokarin.aqloss/data /app/share/xyz.nokarin.aqloss/data
      - mkdir /app/lib
      - cp -r $BUNDLE_PATH/lib/xyz.nokarin.aqloss /app/lib/xyz.nokarin.aqloss
      - install -Dm755 $BUNDLE_PATH/bin/Aqloss /app/bin/Aqloss
      - install -Dm644 linux/xyz.nokarin.aqloss.desktop /app/share/applications/xyz.nokarin.aqloss.desktop
      - install -Dm644 linux/xyz.nokarin.aqloss.metainfo.xml /app/share/metainfo/xyz.nokarin.aqloss.metainfo.xml
      - install -Dm644 assets/icons/icon_64.png /app/share/icons/hicolor/64x64/apps/xyz.nokarin.aqloss.png
      - install -Dm644 assets/icons/icon_128.png /app/share/icons/hicolor/128x128/apps/xyz.nokarin.aqloss.png
      - install -Dm644 assets/icons/icon_256.png /app/share/icons/hicolor/256x256/apps/xyz.nokarin.aqloss.png
      - install -Dm644 assets/icons/icon_512.png /app/share/icons/hicolor/512x512/apps/xyz.nokarin.aqloss.png
    sources:
      - type: git
        url: https://github.com/nokarin-dev/Aqloss.git
        commit: 86ab69d3d10f5ac5b87e679ff670d22321e456a5
      - generated/sources/cargo.json
      - generated/sources/pubspec.json
    modules:
      - generated/modules/rustup-1.94.0.json
      - generated/modules/flutter-sdk-3.41.9.json
