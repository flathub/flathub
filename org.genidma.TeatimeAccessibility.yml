app-id: org.genidma.TeatimeAccessibility
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: /app/bin/teatime-accessibility
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --filesystem=home
  - --share=network

modules:
  - name: teatime-accessibility
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/genidma/teatime-accessibility
        branch: main
    build-commands:
      - mkdir -p /app/bin
      - cp bin/teatime.py /app/bin/teatime.py
      - chmod +x /app/bin/teatime.py
      - printf '#!/bin/bash\nexec /usr/bin/python3 /app/bin/teatime.py "$@"\n' > /app/bin/teatime-accessibility
      - chmod +x /app/bin/teatime-accessibility
      - mkdir -p /app/assets
      - cp -r assets/* /app/assets/
      # Install standard metadata for Flathub
      - install -Dm644 org.genidma.TeatimeAccessibility.desktop /app/share/applications/org.genidma.TeatimeAccessibility.desktop
      - install -Dm644 org.genidma.TeatimeAccessibility.metainfo.xml /app/share/metainfo/org.genidma.TeatimeAccessibility.metainfo.xml
      - install -Dm644 assets/teatime-icon.png /app/share/icons/hicolor/128x128/apps/org.genidma.TeatimeAccessibility.png
      - install -Dm644 assets/teatime-icon.svg /app/share/icons/hicolor/scalable/apps/org.genidma.TeatimeAccessibility.svg