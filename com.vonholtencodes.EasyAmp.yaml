# Flathub-ready manifest for EasyAmp.
#
# Differences from ../com.vonholtencodes.EasyAmp.yaml (the local dev manifest):
#   * the app is built from the pinned git tag, not a local directory, so the
#     build is reproducible from a committed ref (required by Flathub);
#   * permissions are kept minimal (file access goes through the portal).
#
# To submit: copy this file to a flathub/flathub PR branch named after the
# app id, build/lint locally first (see packaging/README-flathub.md), and open
# the PR yourself against the `new-pr` base branch.
app-id: com.vonholtencodes.EasyAmp
runtime: org.gnome.Platform
runtime-version: '50'
sdk: org.gnome.Sdk
command: easyamp

finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --socket=pulseaudio          # playback + monitor capture for the visualizer
  - --filesystem=xdg-music:ro    # read the music library; other files via portal

modules:
  - name: python3-numpy
    buildsystem: simple
    build-commands:
      - pip3 install --no-index --no-build-isolation --prefix=${FLATPAK_DEST} numpy-*.whl
    sources:
      - type: file
        only-arches:
          - x86_64
        url: https://files.pythonhosted.org/packages/a5/9d/3584b9984ca4c047aea75214ce1a4c4c73d849bd71b604264b7f5653f8a8/numpy-2.4.6-cp313-cp313-manylinux_2_27_x86_64.manylinux_2_28_x86_64.whl
        sha256: a7830bab239b79cda9c08c2da014761cafb48da6150e1da17ac06283f43b6089
      - type: file
        only-arches:
          - aarch64
        url: https://files.pythonhosted.org/packages/bb/c5/7b863a97a91671a0338f4253bd3b5a3d3852f0692dae91711c9f4a10e787/numpy-2.4.6-cp313-cp313-manylinux_2_27_aarch64.manylinux_2_28_aarch64.whl
        sha256: 72fbe16c6fac95aedf5937fa873445cec2110be35d8a4e9433d7501fd98dae6b

  - name: easyamp
    buildsystem: simple
    build-commands:
      - pip3 install --no-index --no-build-isolation --no-deps --prefix=${FLATPAK_DEST} .
      - install -Dm644 data/com.vonholtencodes.EasyAmp.desktop
        ${FLATPAK_DEST}/share/applications/com.vonholtencodes.EasyAmp.desktop
      - install -Dm644 data/com.vonholtencodes.EasyAmp.metainfo.xml
        ${FLATPAK_DEST}/share/metainfo/com.vonholtencodes.EasyAmp.metainfo.xml
      - install -Dm644 assets/icons/easyamp-256.png
        ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/com.vonholtencodes.EasyAmp.png
      - install -Dm644 assets/icons/easyamp-128.png
        ${FLATPAK_DEST}/share/icons/hicolor/128x128/apps/com.vonholtencodes.EasyAmp.png
      - install -Dm644 assets/easyamp-logo.svg
        ${FLATPAK_DEST}/share/icons/hicolor/scalable/apps/com.vonholtencodes.EasyAmp.svg
      - install -Dm644 easyamp/fonts/DSEG7Classic-Regular.ttf
        ${FLATPAK_DEST}/share/fonts/easyamp/DSEG7Classic-Regular.ttf
      - install -Dm644 easyamp/fonts/DSEG7Classic-Bold.ttf
        ${FLATPAK_DEST}/share/fonts/easyamp/DSEG7Classic-Bold.ttf
      - install -Dm644 easyamp/fonts/PixelifySans.ttf
        ${FLATPAK_DEST}/share/fonts/easyamp/PixelifySans.ttf
    sources:
      - type: git
        url: https://github.com/VonHoltenCodes/EasyAmp.git
        tag: v0.3.1
        commit: 237a52abc1e09e7b8272f0235639e5d134034712
