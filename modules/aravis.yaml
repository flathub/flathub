name: aravis
modules:

- name: aravis
  buildsystem: meson
  builddir: true
  config-opts:
    - --libdir=lib
    - -Dviewer=disabled
    - -Dgst-plugin=enabled
  sources:
    - type: git
      url: https://github.com/AravisProject/aravis.git
      tag: '0.8.31'
