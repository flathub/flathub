{
  "//": "Generates cpp-httplib.pc because lemonade-cpp's cmake uses pkg_check_modules(cpp-httplib). Cleanup runs at bundle-assembly time (after all modules build), so the .pc is visible to consumers and removed from the final image — don't move the cleanup or the post-install.",
  "name": "cpp-httplib",
  "buildsystem": "cmake-ninja",
  "config-opts": [
    "-DHTTPLIB_REQUIRE_OPENSSL=OFF",
    "-DHTTPLIB_USE_OPENSSL_IF_AVAILABLE=OFF",
    "-DHTTPLIB_REQUIRE_ZLIB=OFF",
    "-DHTTPLIB_USE_ZLIB_IF_AVAILABLE=OFF",
    "-DHTTPLIB_REQUIRE_BROTLI=OFF",
    "-DHTTPLIB_USE_BROTLI_IF_AVAILABLE=OFF",
    "-DHTTPLIB_REQUIRE_ZSTD=OFF",
    "-DHTTPLIB_USE_ZSTD_IF_AVAILABLE=OFF",
    "-DHTTPLIB_COMPILE=ON",
    "-DBUILD_SHARED_LIBS=OFF",
    "-DHTTPLIB_TEST=OFF"
  ],
  "post-install": [
    "install -d /app/lib/pkgconfig",
    "printf 'Name: cpp-httplib\\nDescription: A C++ HTTP/HTTPS server and client library\\nVersion: 0.26.0\\nCflags: -I/app/include\\nLibs: -L/app/lib -lcpp-httplib\\n' > /app/lib/pkgconfig/cpp-httplib.pc"
  ],
  "cleanup": [ "/include", "/lib/cmake", "/lib/pkgconfig" ],
  "sources": [
    {
      "type": "git",
      "url": "https://github.com/yhirose/cpp-httplib.git",
      "tag": "v0.26.0",
      "commit": "89c932f313c6437c38f2982869beacc89c2f2246"
    }
  ]
}
