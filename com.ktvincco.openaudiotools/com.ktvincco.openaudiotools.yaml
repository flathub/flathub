app-id: com.ktvincco.openaudiotools
runtime: org.freedesktop.Platform
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
command: OpenAudioTools

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --device=all
  - --filesystem=xdg-run/pipewire-0

modules:
  - name: openaudiotools
    buildsystem: simple
    build-commands:
      # Extract the debian package
      - ar x openaudiotools.deb
      - tar xf data.tar.zst
      
      # The deb usually installs to /opt/openaudiotools
      # We move everything to /app/ which is the Flatpak standard
      - mkdir -p /app/bin /app/share/applications /app/share/icons/hicolor/scalable/apps
      
      # Move the actual application files
      - cp -r opt/openaudiotools/* /app/
      
      # Create a symlink to the binary in /app/bin so it's in the PATH
      - ln -s /app/bin/OpenAudioTools /app/bin/openaudiotools
      
      # Install the desktop file and icons
      - install -Dm644 com.ktvincco.openaudiotools.desktop /app/share/applications/com.ktvincco.openaudiotools.desktop
      - install -Dm644 com.ktvincco.openaudiotools.metainfo.xml /app/share/metainfo/com.ktvincco.openaudiotools.metainfo.xml
      
      # Note: You should also include your icon. Check where your deb puts it or add it as a source
      - install -Dm644 icon.png /app/share/icons/hicolor/256x256/apps/com.ktvincco.openaudiotools.png

    sources:
      - type: file
        url: https://github.com/ketaslava/open_audio_tools_app/releases/download/2.5.0/openaudiotools_2.5.0_amd64.deb
        sha256: d1422efca4c9e168ef6ecf23ab9fe046aa67e1ff8062fc4a57dc925269321f4c
        dest-filename: openaudiotools.deb
      
      - type: file
        path: com.ktvincco.openaudiotools.desktop
      
      - type: file
        path: com.ktvincco.openaudiotools.metainfo.xml
        
      - type: file
        path: icon.png
