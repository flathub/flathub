# yaml-language-server: $schema=https://raw.githubusercontent.com/flatpak/flatpak-builder/main/data/flatpak-manifest.schema.json

app-id: com.surfshark.Surfshark
runtime: org.gnome.Platform
runtime-version: '45'
base: org.electronjs.Electron2.BaseApp
base-version: '23.08'
sdk: org.gnome.Sdk
command: surfshark
separate-locales: false
finish-args:
  - --socket=x11
  - --share=ipc
  - --share=network
  - --system-talk-name=org.freedesktop.NetworkManager
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.kde.StatusNotifierWatcher
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons
modules:
  - 'libndp.yaml'
  - 'network-manager.yaml'
  - 'libnma.yaml'
  - 'network-manager-openvpn.yaml'
  - name: surfshark
    buildsystem: simple
    build-commands:
      - ar -x surfshark_*.deb
      - rm -f surfshark_*.deb
      - tar -xf data.tar.xz

      - install surfshark $FLATPAK_DEST/bin/
      - install -D "usr/share/applications/surfshark.desktop" "${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop"
      - install -D "usr/share/icons/hicolor/128x128/apps/surfshark.png" "${FLATPAK_DEST}/share/icons/hicolor/128x128/apps/${FLATPAK_ID}.png"
      - install -D "usr/share/icons/hicolor/256x256/apps/surfshark.png" "${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png"
      - install -D "usr/share/icons/hicolor/512x512/apps/surfshark.png" "${FLATPAK_DEST}/share/icons/hicolor/512x512/apps/${FLATPAK_ID}.png"
      - install -D com.surfshark.Surfshark.metainfo.xml "${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml"

      - cp -r opt $FLATPAK_DEST
      - cp -r usr/* $FLATPAK_DEST
      - cp -r var $FLATPAK_DEST

      - desktop-file-edit --set-key=Icon --set-value="${FLATPAK_ID}" "${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop"
      - desktop-file-edit --set-key=Exec --set-value="surfshark %U" "${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop"
    sources:
      - type: script
        dest-filename: surfshark
        commands:
        - TMPDIR="$XDG_RUNTIME_DIR/app/$FLATPAK_ID" zypak-wrapper /app/opt/Surfshark/surfshark "$@"
      - type: file
        path: com.surfshark.Surfshark.metainfo.xml
#
      - type: file
        only-arches:
          - x86_64
        url: https://ocean.surfshark.com/debian/pool/main/s/surfshark_3.0.3_amd64.deb
        sha256: 534a46d0994f28cca0155efc625d949d810221d3d244681dc6b8f4b46b1efaf8
        x-checker-data:
          type: anitya
          project-id: 372017
          is-main-source: true
          url-template: https://ocean.surfshark.com/debian/pool/main/s/surfshark_${version}_amd64.deb
      - type: file
        only-arches:
          - aarch64
        url: https://ocean.surfshark.com/debian/pool/main/s/surfshark_3.0.3_arm64.deb
        sha256: 7ed2100c19ddedc17ed059296a6ac602cf27b3d8bbf619d3235fd93c3e47a2bb
        x-checker-data:
          type: anitya
          project-id: 372017
          is-main-source: true
          url-template: https://ocean.surfshark.com/debian/pool/main/s/surfshark_${version}_arm64.deb
#

