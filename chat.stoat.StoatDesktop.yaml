app-id: chat.stoat.StoatDesktop
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "25.08"
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node24
command: stoat-desktop
separate-locales: false
finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  # Required to provide Call functionality
  - --socket=pulseaudio
  - --device=all
  # Network access
  - --share=network
  # Local filesystem
  - --filesystem=xdg-videos:ro
  - --filesystem=xdg-pictures:ro
  - --filesystem=xdg-download
  - --filesystem=xdg-run/pipewire-0
  - --filesystem=xdg-run/speech-dispatcher
  - --talk-name=org.freedesktop.ScreenSaver
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=com.canonical.AppMenu.Registrar
  - --talk-name=com.canonical.indicator.application
  - --talk-name=com.canonical.Unity
    # Required for notifications in various desktop environments
  - --talk-name=org.kde.StatusNotifierWatcher
  - --system-talk-name=org.freedesktop.UPower
  # Ensure that custom mouse cursors is used
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons
  - --env=ELECTRON_TRASH=gio
build-options:
  append-path: /usr/lib/sdk/node24/bin
  env:
    NPM_CONFIG_LOGLEVEL: info
    XDG_CACHE_HOME: /run/build/stoat/flatpak-node/cache
    npm_config_offline: 'true'
    npm_config_cache: /run/build/stoat/flatpak-node/npm-cache
    npm_config_loglevel: verbose
    ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
    ELECTRON_FORCE_IS_VERSIONED_INSTALLATION: '1'
    electron_config_cache: /run/build/stoat/flatpak-node/npm-cache

modules:
  # Build and install stoat
  - name: stoat
    buildsystem: simple
    build-options:
      env:
        npm_config_offline: 'true'
        npm_config_cache: /run/build/stoat/flatpak-node/npm-cache
        electron_zip_dir: /run/build/stoat/flatpak-node/npm-cache/electron
        XDG_CACHE_HOME: /run/build/stoat/flatpak-node/cache
        PLATFORM: ci
    build-commands:
      - install -dm755 "${FLATPAK_DEST}/bin" "${FLATPAK_DEST}/stoat-desktop"

      - npm install --legacy-peer-deps --lockfile-version 1 --force --offline > /dev/null

      - install -Dm755 pnpm /app/bin/pnpm
      - pnpm exec electron-packager . stoat-chat --electronZipDir='/run/build/stoat/flatpak-node/cache/electron'
      - cp -r stoat-chat-linux-x64/. /app/stoat-desktop/

      # Install wrapper
      - install -Dm755 stoat-desktop.sh /app/bin/stoat-desktop

      # Desktop + metadata + icon
      - install -D chat.stoat.StoatDesktop.desktop -t /app/share/applications/
      - install -Dm644 chat.stoat.StoatDesktop.metainfo.xml -t /app/share/metainfo/
      - install -D assets/desktop/icon.png /app/share/icons/hicolor/256x256/apps/chat.stoat.StoatDesktop.png
    sources:
      - type: file
        path: package-lock.json
      - type: archive
        url: https://github.com/stoatchat/for-desktop/archive/refs/tags/v1.2.0.tar.gz
        sha256: abfdb8f30c7b3ec0ddabc92f69ad21162bb53e457ec88c4188c64f0d293b63cb
        dest: .
      - type: git
        url: https://github.com/stoatchat/assets
        commit: f106946659af67ad4f008588ac51570029b2fd47
        dest: 'assets/'
      - generated-sources.json
        # Inspiration taken from https://github.com/RushingAlien/vesktop-flatpak/blob/build-from-source/dev.vencord.Vesktop.yml
      - type: file
        url: https://github.com/pnpm/pnpm/releases/download/v8.10.5/pnpm-linux-x64
        sha256: 92b2da9a8b2c669ae3741a52dde8f8b694330dbd4cf49bfb331f2454e98556fb
        dest-filename: pnpm
      - type: file
        path: .vite/build/main.js
        dest: .vite/build/
      - type: file
        path: .vite/build/preload.js
        dest: .vite/build/
      - type: file
        path: chat.stoat.StoatDesktop.desktop
      - type: file
        path: chat.stoat.StoatDesktop.metainfo.xml
      - type: script
        dest-filename: stoat-desktop.sh
        commands:
          # TMPDIR env variable fixes the tray icon issue (#6)
          - export TMPDIR="$XDG_RUNTIME_DIR/app/${FLATPAK_ID:-chat.stoat.StoatDesktop}"
          - WAYLAND_FLAGS=""
          - if [ -n "$WAYLAND_DISPLAY" ]; then WAYLAND_FLAGS="--enable-features=UseOzonePlatform,WaylandWindowDecorations --ozone-platform=wayland"; fi
          - exec zypak-wrapper /app/stoat-desktop/stoat-chat $WAYLAND_FLAGS "$@"
