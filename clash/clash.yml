name: clash
sources:
  - type: git
    url: https://github.com/Dreamacro/clash
    tag: v1.17.0
    x-checker-data:
      type: git
  - go.mod.yml
buildsystem: simple
build-commands:
  - go build -ldflags="-w -s" -o clash
  - cp clash /app/bin
build-options:
  append-path: /usr/lib/sdk/golang/bin
