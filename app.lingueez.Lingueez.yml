id: app.lingueez.Lingueez
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
base: io.qt.PySide.BaseApp
base-version: '6.10'
command: lingueez

finish-args:
  # Account sync/auth, translation, text-to-speech, RSS, web article fetch.
  - --share=network
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  # Audio for read-aloud playback.
  - --socket=pulseaudio
  - --device=dri
  # System tray (StatusNotifierItem).
  - --talk-name=org.kde.StatusNotifierWatcher
  # "Report an issue" writes a diagnostics bundle to Downloads.
  - --filesystem=xdg-download

modules:
  # Python dependencies (PySide6 is provided by the base app).
  - python3-deps.json

  # ffmpeg is provided by the runtime (/usr/bin/ffmpeg, with libmp3lame); not bundled.

  - name: lingueez
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/lysak-yurii/lingueez.git
        commit: e520824e27541f4b88c4afdc9f8426395e79e985
