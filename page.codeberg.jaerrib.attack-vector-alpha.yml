id: page.codeberg.jaerrib.attack-vector-alpha
runtime: org.freedesktop.Platform
runtime-version: '25.08'
base: org.godotengine.godot.BaseApp
base-version: '3.6-25.08'
sdk: org.freedesktop.Sdk
command: godot-runner

finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=pulseaudio
  - --device=all

modules:
  - name: attackvectoralpha
    buildsystem: simple

    sources:
      - type: git
        url: https://codeberg.org/jaerrib/attack-vector-alpha.git
        commit: 7be34c292710e078e5628f92435f2f5ad10f36f9

      - type: file
        url: https://codeberg.org/jaerrib/attack-vector-alpha/releases/download/1.0/Attack_Vector_Alpha.pck
        sha256: 0e1e9a4b652352f56b1e182d41a0e2cca2c903bb987e81ccaae53ecbec1e0195

    build-commands:
      - install -Dm644 Attack_Vector_Alpha.pck ${FLATPAK_DEST}/bin/godot-runner.pck
      - install -Dm644 ${FLATPAK_ID}.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm644 ${FLATPAK_ID}.xml ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm644 ${FLATPAK_ID}.png ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
