name: xdg-utils
modules:
  - name: xdg-utils
    buildsystem: meson
    cleanup:
      - /bin/flatpak-spawn
      - /bin/xdg-email
    sources:
      - type: git
        url: https://github.com/flatpak/flatpak-xdg-utils.git
        tag: 1.0.5
