app-id: io.github.tobagin.Tilepaint
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk
command: tilepaint
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
modules:
  - name: tilepaint
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tobagin/Tilepaint.git
        tag: 1.0.0
        commit: 6db72c8f8f27606bbf4957fb88d8765113a7bac1
