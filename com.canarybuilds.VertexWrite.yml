app-id: com.canarybuilds.VertexWrite
runtime: org.gnome.Platform
runtime-version: "50"
sdk: org.gnome.Sdk
command: vertexwrite

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --filesystem=xdg-documents
  - --filesystem=xdg-download

modules:
  - name: gtksourceview4
    buildsystem: meson
    config-opts:
      - --libdir=lib
      - -Dgtk_doc=false
      - -Dgir=true
      - -Dvapi=false
      - -Dinstall_tests=false
    sources:
      - type: archive
        url: https://download.gnome.org/sources/gtksourceview/4.8/gtksourceview-4.8.4.tar.xz
        sha256: 7ec9d18fb283d1f84a3a3eff3b7a72b09a10c9c006597b3fbabbb5958420a87d

  - python3-requirements.json

  - name: vertexwrite
    buildsystem: meson
    config-opts:
      - --libdir=lib
    sources:
      - type: git
        url: https://github.com/Canary-Builds/vertexwrite.git
        tag: v0.7.4
        commit: 4f37030706a0686ca468c58ee76206c07331605a
