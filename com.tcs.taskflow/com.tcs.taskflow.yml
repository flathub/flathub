id: com.tcs.taskflow
runtime: org.gnome.Platform
runtime-version: "48"
sdk: org.gnome.Sdk
command: taskflow

finish-args:
  # Window display permissions
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --share=ipc

  # Network access (if your app needs it)
  - --share=network

  # File system access (adjust as needed)
  - --filesystem=home

  # Optional: Tray icon support
  - --talk-name=org.kde.StatusNotifierWatcher
  - --filesystem=xdg-run/tray-icon:create

  # Optional: Single instance support
  - --own-name=com.tcs.taskflow.SingleInstance

  # Optional: Audio support (if your app plays sounds)
  # - --socket=pulseaudio

  # Optional: Notifications
  # - --talk-name=org.freedesktop.Notifications

modules:
  - name: binary
    buildsystem: simple
    sources:
      # Using the portable binary directly from the build output
      - type: file
        path: ../src-tauri/target/release/taskflow

      # Desktop file - you'll need to create this
      - type: file
        path: com.tcs.taskflow.desktop

      # Icon - adjust path to your actual icon location
      - type: file
        path: ../src-tauri/icons/icon.png

      # AppStream metadata
      - type: file
        path: com.tcs.taskflow.metainfo.xml

    build-commands:
      # Install binary
      - install -Dm755 taskflow /app/bin/taskflow

      # Install desktop file
      - install -Dm644 com.tcs.taskflow.desktop /app/share/applications/com.tcs.taskflow.desktop

      # Install icon (to multiple sizes for better scaling)
      - install -Dm644 icon.png /app/share/icons/hicolor/128x128/apps/com.tcs.taskflow.png
      - install -Dm644 icon.png /app/share/icons/hicolor/256x256/apps/com.tcs.taskflow.png

      # Install AppStream metadata
      - install -Dm644 com.tcs.taskflow.metainfo.xml /app/share/metainfo/com.tcs.taskflow.metainfo.xml