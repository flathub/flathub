#!/usr/bin/bash


#This is literally the executable that that gets ran. It's been a major headache getting this to work so-
#I'm just going to keep the executable binary name as is. Feel free to change it later to something like-
#'writersolo-desktop' or anything you like.
zypak-wrapper '/app/extra/opt/WriterSolo/${productName}-${version}-${arch}.${ext}'
