pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r repo .public
  - cp scribus-nightly.flatpakref scribus.gpg index.html .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
