# Generated by flutpak — https://github.com/o-murphy/flutpak
app-id: ink.openlyst.doudou
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: doudou
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio
  - --share=network
  - --filesystem=xdg-music:ro
  - --filesystem=xdg-download
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=org.mpris.MediaPlayer2.Player
  - --own-name=org.mpris.MediaPlayer2.com.mycompany.myapp.audio
  - --own-name=org.mpris.MediaPlayer2.com.mycompany.myapp.audio.*
modules:
  - ../shared-modules/libappindicator/libappindicator-gtk3-12.10.json
  - mpv.json
  - flutter-sdk-3.38.0.json
  - name: doudou
    buildsystem: simple
    build-options:
      build-args:
        - --share=network
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/flutter/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        PUB_CACHE: /run/build/doudou/.pub-cache
    build-commands:
      - flutter pub get
      - flutter build linux --release --no-pub
      - mkdir -p /app/doudou
      - cp -r "$BUNDLE_PATH"/ /app/doudou/
      - ln -s /app/lib/libmpv.so /app/lib/libmpv.so.1
      - ln -s /app/lib/libappindicator3.so /app/doudou/lib/libayatana-appindicator3.so.1
      - ln -s /app/lib/libindicator3.so /app/doudou/lib/libayatana-indicator3.so.7
      - ln -s /app/lib/libindicator3.so /app/doudou/lib/libayatana-ido3-0.4.so.0
      - install -Dm755 flatpak/doudou-wrapper.sh /app/bin/doudou
      - install -Dm644 app/share/metainfo/ink.openlyst.doudou.metainfo.xml /app/share/metainfo/ink.openlyst.doudou.metainfo.xml
      - install -Dm644 app/share/applications/ink.openlyst.doudou.desktop /app/share/applications/ink.openlyst.doudou.desktop
      - install -Dm644 app/share/icons/hicolor/256x256/apps/ink.openlyst.doudou.png /app/share/icons/hicolor/256x256/apps/ink.openlyst.doudou.png
      - install -Dm644 app/share/icons/hicolor/scalable/apps/ink.openlyst.doudou.svg /app/share/icons/hicolor/scalable/apps/ink.openlyst.doudou.svg
    sources:
      - type: git
        url: https://gitlab.com/Openlyst/doudou.git
        commit: d3fa5c11088bf6dc3b0cba2a9e0c62b519da059c
      - pubspec-sources.json
