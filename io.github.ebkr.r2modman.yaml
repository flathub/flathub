id: io.github.ebkr.r2modman
runtime: org.freedesktop.Platform
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "25.08"
command: r2modman-plus

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri

  - --talk-name=org.freedesktop.Flatpak

  - --share=network
  - --share=ipc

  - --filesystem=host
