name: gpick
buildsystem: simple
builddir: true
sources:
  # Source0
  - type: archive
    url: https://github.com/thezbyg/gpick/archive/gpick-0.2.5.tar.gz
    sha256: 3a9706c101f82e9a49880db9fb408c760d49e6b4506ee5768962c25ec2ecbb57
  # Source1
  # Build config
  - type: file
    path: user-config.py
  # Source2
  # AppData file from Git repo
  - type: file
    url: https://raw.githubusercontent.com/thezbyg/gpick/gpick-0.2.6rc1/share/appdata/gpick.appdata.xml
    sha256: 94445ad9ed145c190bda5f054c0c0ffa1db8ac57666b377551579ae3695a6b84
  # Delete external libraries
  - type: shell
    commands:
      - rm -rf "extern"
build-commands:
  - scons -j"${FLATPAK_BUILDER_N_JOBS}";
  - scons prefix="${FLATPAK_DEST}" install;
post-install:
  - install -p -D -m 0644 "gpick.appdata.xml" -t "${FLATPAK_DEST}/share/metainfo/";
