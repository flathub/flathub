#!/bin/sh
# Runs at install time inside the sandbox: unpack the downloaded AppImage
# (self-extracting, no FUSE needed) into /app/extra/app.
set -e
chmod +x restorm.AppImage
./restorm.AppImage --appimage-extract > /dev/null
rm restorm.AppImage
mv squashfs-root app
