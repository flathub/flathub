#!/bin/sh
# Run by the flatpak client at install time, after extra-data is downloaded.
# Extracts the application payload into /app/extra/aiche-desktop/ and removes
# the original tarball.
set -e

cd /app/extra
tar xzf aiche-desktop.tar.gz
rm aiche-desktop.tar.gz
