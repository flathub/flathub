# Generated by flatpak-flutter v0.13.0 from cat.radiotrama.radio_trama.yml, do not edit
# Visit the project at https://github.com/TheAppgineer/flatpak-flutter
app-id: cat.radiotrama.radio_trama
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
command: radio-trama
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm21
build-options:
  append-path: /usr/lib/sdk/llvm21/bin:/var/lib/flutter/bin
  prepend-ld-library-path: /usr/lib/sdk/llvm21/lib
  env:
    PUB_CACHE: /run/build/radio_trama/.pub-cache
  arch:
    x86_64:
      env:
        BUNDLE_PATH: build/linux/x64/release/bundle
    aarch64:
      env:
        BUNDLE_PATH: build/linux/arm64/release/bundle
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --socket=pulseaudio
  - --share=network
  - --device=dri
modules:
  - name: radio_trama
    buildsystem: simple
    build-commands:
      - setup-flutter.sh
      - flutter build linux --release
      - mkdir -p /app/bin
      - cp -r $BUNDLE_PATH/* /app/bin/
      - ln -s /app/bin/radio_trama /app/bin/radio-trama
      - install -Dm644 flatpak/cat.radiotrama.radio_trama.desktop -t /app/share/applications/
      - install -Dm644 flatpak/cat.radiotrama.radio_trama.metainfo.xml -t /app/share/metainfo/
      - install -Dm644 flatpak/cat.radiotrama.radio_trama.png -t /app/share/icons/hicolor/256x256/apps/
    sources:
      - type: git
        url: https://gitlab.com/RikRuk/radio_trama.git
        commit: 4377b1c9e02e721aa033fe9848eecfce1deb6ea2
      - generated/sources/pubspec.json
    modules:
      - generated/modules/flutter-sdk-3.38.9.json
