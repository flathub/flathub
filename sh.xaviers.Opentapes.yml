app-id: sh.xaviers.Opentapes
command: opentapes
runtime: org.gnome.Platform
runtime-version: '41'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node16
  - org.freedesktop.Sdk.Extension.rust-stable
rename-icon: opentapes
finish-args:
  - --device=dri
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --share=network
build-options:
  append-path: /usr/lib/sdk/node16/bin
  prepend-path: /usr/lib/sdk/rust-stable/bin
  build-args:
    - --share=network
  env:
    NPM_CONFIG_LOGLEVEL: info
    CARGO_HOME: /run/build/Opentapes/cargo
    RUST_BACKTRACE: '1'
cleanup:
  - /node_modules
  - /dist
  - /src-tauri/target
modules:
  - name: Opentapes
    sources:
      - type: archive
        url: https://codeberg.org/xaviers/Opentapes/archive/0.1.0.tar.gz
        sha256: e6ccd9fae94b581d5b125edb44196c26e902157c19dd062daac57bc7a69b16b3
    buildsystem: simple
    build-commands:
      - npm install
      - npm run build
      - cargo install tauri-cli --version 1.0.0-beta.7
      - cargo tauri build
      - mkdir -p /app/share/applications
      - mkdir -p /app/share/metainfo
      - install -Dm644 -t /app/share/applications/ resources/sh.xaviers.Opentapes.desktop
      - install -Dm644 -t /app/share/metainfo/ resources/sh.xaviers.Opentapes.appdata.xml
      # Remove the 512x512@2x icon as it has an incorrect size (issue with Tauri's bundler)
      - rm -r src-tauri/target/release/bundle/deb/*/data/usr/share/icons/hicolor/512x512@2x
      - cp -R src-tauri/target/release/bundle/deb/*/data/usr/share/icons /app/share/
      - install -Dm755 -t /app/bin/ src-tauri/target/release/bundle/deb/*/data/usr/bin/opentapes