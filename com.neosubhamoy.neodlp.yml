id: com.neosubhamoy.neodlp

runtime: org.gnome.Platform
runtime-version: "49"
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
  - org.freedesktop.Sdk.Extension.node24

command: neodlp
finish-args:
  - --socket=wayland # Show app window on wayland
  - --socket=fallback-x11 # Fallback to X11 if wayland is not available
  - --device=dri # Use OpenGL
  - --share=ipc # Needed for good X11 performance
  - --share=network # Allow network access
  - --filesystem=xdg-download # Allow access to downloads folder for storing downloaded files
  - --filesystem=~/.config/BraveSoftware/Brave-Browser:ro # Allow read-only access to following browser configs for yt-dlp cookie extraction
  - --filesystem=~/.config/google-chrome:ro
  - --filesystem=~/.config/chromium:ro
  - --filesystem=~/.config/microsoft-edge:ro
  - --filesystem=~/.mozilla/firefox:ro
  - --filesystem=~/.config/mozilla/firefox:ro
  - --filesystem=~/snap/firefox/common/.mozilla/firefox:ro
  - --filesystem=~/.config/opera:ro
  - --filesystem=~/.config/vivaldi:ro
  - --filesystem=~/.config/naver-whale:ro
  - --env=FLATPAK=1 # Used to determine flatpak environment on app side
  - --talk-name=org.kde.StatusNotifierWatcher # Needed for showing tray icon

build-options:
  append-path: /usr/lib/sdk/node24/bin:/usr/lib/sdk/rust-stable/bin

modules:
  - shared-modules/libappindicator/libappindicator-gtk3-12.10.json

  - name: aria2
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/aria2/aria2.git
        commit: b4fd7cb1ca03e38ad9d7ab9308b8200cb1d41c25

    build-commands:
      - autoreconf -i
      - ./configure --prefix=/app --with-ca-bundle=/etc/ssl/certs/ca-certificates.crt
      - make -j $FLATPAK_BUILDER_N_JOBS
      - make install

  - name: neodlp-pot
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/jim60105/bgutil-ytdlp-pot-provider-rs.git
        tag: v0.7.2
        commit: ed6fc38eb6ea6755979b7cba9507888ea167c4a2
      # Rust package sources -> python3 flatpak-cargo-generator.py Cargo.lock -o flatpak/neodlp-pot-cargo-sources.json
      - neodlp-pot-cargo-sources.json
      # Get rusty_v8 prebuilt static library (to avoid building it from source which is very resource intensive)
      - type: file
        url: https://github.com/denoland/rusty_v8/releases/download/v130.0.7/librusty_v8_release_x86_64-unknown-linux-gnu.a.gz
        sha256: a6476cb94e9b0247081c465424eb793d77732b8db80848132d78cbb50f34b75d
        only-arches:
          - x86_64
        dest-filename: rusty_v8-x86_64.a.gz
      - type: file
        url: https://github.com/denoland/rusty_v8/releases/download/v130.0.7/librusty_v8_release_aarch64-unknown-linux-gnu.a.gz
        sha256: beefe7b35abee77159f7cb55096666b07630c11047d5f3a74dd065863729915a
        only-arches:
          - aarch64
        dest-filename: rusty_v8-aarch64.a.gz

    build-options:
      env:
        CARGO_HOME: /run/build/neodlp-pot/cargo
        XDG_CACHE_HOME: /run/build/neodlp-pot/flatpak-node/cache

    build-commands:
      # Install Rust packages offline
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      # Move rusty_v8 static library to cargo registry location so that it can be used during build
      - |
        if [ "$(uname -m)" = "x86_64" ]; then
          mkdir -p /run/build/neodlp-pot/cargo/.rusty_v8/https___github_com_denoland_rusty_v8_releases_download_v130_0_7_librusty_v8_release_x86_64_unknown_linux_gnu_a_gz
          mv rusty_v8-x86_64.a.gz /run/build/neodlp-pot/cargo/.rusty_v8/https___github_com_denoland_rusty_v8_releases_download_v130_0_7_librusty_v8_release_x86_64_unknown_linux_gnu_a_gz/librusty_v8_release_x86_64-unknown-linux-gnu.a.gz
        elif [ "$(uname -m)" = "aarch64" ]; then
          mkdir -p /run/build/neodlp-pot/cargo/.rusty_v8/https___github_com_denoland_rusty_v8_releases_download_v130_0_7_librusty_v8_release_aarch64_unknown_linux_gnu_a_gz
          mv rusty_v8-aarch64.a.gz /run/build/neodlp-pot/cargo/.rusty_v8/https___github_com_denoland_rusty_v8_releases_download_v130_0_7_librusty_v8_release_aarch64_unknown_linux_gnu_a_gz/librusty_v8_release_aarch64-unknown-linux-gnu.a.gz
        fi
      # Run offline build
      - cargo --offline build --release
      # Move built binary to /app/bin
      - mv target/release/bgutil-pot /app/bin/neodlp-pot
  
  - name: neodlp
    buildsystem: simple
    sources:
      - type: git
        url: https://github.com/neosubhamoy/neodlp.git
        branch: main
      # Node.js package sources -> flatpak-node-generator npm -o flatpak/node-sources.json package-lock.json
      - node-sources.json
      # Rust package sources -> python3 flatpak-cargo-generator.py src-tauri/Cargo.lock -o flatpak/cargo-sources.json
      - cargo-sources.json
      # Tauri sidecar binary sources
      - sidecar-sources.json

    build-options:
      env:
        CARGO_HOME: /run/build/neodlp/cargo
        XDG_CACHE_HOME: /run/build/neodlp/flatpak-node/cache
        npm_config_cache: /run/build/neodlp/flatpak-node/npm-cache
        npm_config_offline: "true"

    build-commands:
      # Install Node.js packages offline
      - npm ci --offline
      # Install Rust packages offline
      - cargo --offline fetch --manifest-path src-tauri/Cargo.toml --verbose
      # Make sidecar binaries executable
      - chmod +x src-tauri/binaries/*
      # Run offline tauri build without bundling
      - npm run --offline tauri build -- --no-bundle --config src-tauri/tauri.linux-flatpak.conf.json
      
      # Install app
      - install -Dm644 -t /app/share/metainfo/ com.neosubhamoy.neodlp.metainfo.xml
      - install -Dm644 -t /app/share/applications/ com.neosubhamoy.neodlp.desktop
      - install -Dm755 -t /app/bin/ src-tauri/target/release/neodlp
      - install -Dm755 -t /app/bin/ src-tauri/target/release/yt-dlp
      - install -Dm755 -t /app/bin/ src-tauri/target/release/deno

      # Install yt-dlp-plugins
      - cp -rf src-tauri/resources/plugins/yt-dlp-plugins /app/bin

      # Install icons
      - install -Dm644 src-tauri/icons/32x32.png /app/share/icons/hicolor/32x32/apps/com.neosubhamoy.neodlp.png
      - install -Dm644 src-tauri/icons/128x128.png /app/share/icons/hicolor/128x128/apps/com.neosubhamoy.neodlp.png
      - install -Dm644 src-tauri/icons/128x128@2x.png /app/share/icons/hicolor/256x256/apps/com.neosubhamoy.neodlp.png
      - install -Dm644 src-tauri/icons/icon.png /app/share/icons/hicolor/256x256@2/apps/com.neosubhamoy.neodlp.png

    post-install:
      - install -Dm644 LICENSE /app/share/licenses/com.neosubhamoy.neodlp/LICENSE