id: io.github.getnf.embellish
runtime: org.gnome.Platform
runtime-version: "46"
sdk: org.gnome.Sdk
command: embellish

sdk-extensions:
  - org.freedesktop.Sdk.Extension.golang

finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --share=network
  - --filesystem=~/.local/share/fonts
  - --filesystem=xdg-download:rw
  - --device=dri
modules:
  - name: embellish
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/golang/bin
      env:
        GOBIN: /app/bin
        GOROOT: /usr/lib/sdk/golang
    build-commands:
      - go install -v -tags=terminal,flatpak -ldflags "-s -w" -mod=vendor .
      - mv $GOBIN/embellish $GOBIN/embellish-tui
      - go install -v -tags=gui,flatpak -ldflags "-s -w" -mod=vendor .
      - install -Dm644 $FLATPAK_ID.desktop /app/share/applications/$FLATPAK_ID.desktop
      - install -Dm644 $FLATPAK_ID.svg /app/share/icons/hicolor/scalable/apps/$FLATPAK_ID.svg
      - install -Dm644 $FLATPAK_ID.metainfo.xml /app/share/metainfo/$FLATPAK_ID.metainfo.xml
    sources:
      - type: archive
        url: https://github.com/getnf/embellish/archive/refs/tags/v0.2.3.tar.gz
        sha256: 43c06d28574191e2fefb9bd7b55cecbbbb6a3310bee5ff122c79bba60cade3ae

      - go.mod.yml
