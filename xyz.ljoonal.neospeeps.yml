app-id: xyz.ljoonal.neospeeps
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
  - org.freedesktop.Sdk.Extension.llvm13
command: neos_peeps
finish-args:
  # Needs to talk to the network:
  - --share=network
  # Wayland access
  - --socket=wayland
  # X11 + XShm access
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin:/usr/lib/sdk/llvm13/bin
  env: 
    CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER: clang
    CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_RUSTFLAGS: -C link-arg=-fuse-ld=/usr/lib/sdk/rust-stable/bin/mold
    CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: clang,
    CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS: -C link-arg=-fuse-ld=/usr/lib/sdk/rust-stable/bin/mold
modules:
  - name: neos_peeps
    buildsystem: simple
    build-options:
      env:
        CARGO_HOME: /run/build/neos_peeps/cargo
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      - cargo --offline build --release --verbose
      - install -D target/release/neos_peeps /app/bin/neos_peeps
      - cp *.md /app/
      - mkdir -p /app/share/metainfo/
      - cp static/xyz.ljoonal.neospeeps.appdata.xml /app/share/metainfo/
      - mkdir -p /app/share/icons/hicolor/512x512/apps/ /app/share/icons/hicolor/128x128/apps/ /app/share/icons/hicolor/64x64/apps/ /app/share/applications/
      - cp static/logo-0512.png /app/share/icons/hicolor/512x512/apps/xyz.ljoonal.neospeeps.png
      - cp static/logo-0128.png /app/share/icons/hicolor/128x128/apps/xyz.ljoonal.neospeeps.png
      - cp static/logo-0064.png /app/share/icons/hicolor/64x64/apps/xyz.ljoonal.neospeeps.png
      - cp static/neos_peeps.desktop /app/share/applications/xyz.ljoonal.neospeeps.desktop
    sources:
      - type: git
        url: https://git.ljoonal.xyz/ljoonal/NeosPeeps.git
        tag: v0.4.3
        commit: 6c4f6f18a2ce004300bc0204c7f009c355d6c800
      - type: file
        path: xyz.ljoonal.neospeeps.appdata.xml
      - generated-sources.json
