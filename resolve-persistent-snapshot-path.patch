--- a/simpliplay/main.js
+++ b/simpliplay/main.js
@@ -6,1 +6,4 @@
-    const snapshotsDir = path.join(os.homedir(), 'simpliplay-snapshots');
+    const isFlatpak = !!process.env.FLATPAK_ID;
+    const snapshotsDir = isFlatpak
+      ? path.join(os.homedir(), '.var/app', process.env.FLATPAK_ID, 'simpliplay-snapshots')
+      : path.join(os.homedir(), 'simpliplay-snapshots');
