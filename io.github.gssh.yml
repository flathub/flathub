app-id: io.github.gssh
runtime: org.gnome.Platform
runtime-version: '44'
sdk: org.gnome.Sdk
command: runner.sh
finish-args:
  - --share=network
  - --socket=x11
  - --socket=wayland
  - --share=ipc
  - --socket=system-bus
  - --talk-name=org.freedesktop.Flatpak
modules:
  - name: Gssh
    buildsystem: simple
    build-commands:
      - install -D Flatpak/runner.sh /app/bin/runner.sh
      - install -D main.py /app/bin/main.py
      - install -D Flatpak/.desktop /app/share/applications/io.github.gssh.desktop
      - install -D Flatpak/appicon.png /app/share/icons/hicolor/128x128/apps/io.github.gssh.appicon.png
      - install -D Flatpak/io.github.gssh.appdata.xml /app/share/metainfo/io.github.gssh.appdata.xml

    sources:
      - type: git
        url: https://github.com/gssh-linux/gssh.git
        tag: 1.0.8