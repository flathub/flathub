id: io.github.noobping.listenmoe
runtime: org.gnome.Platform
runtime-version: '49'
sdk: org.gnome.Sdk

sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable

command: listenmoe

finish-args:
  - --device=dri
  - --share=ipc
  - --share=network
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --socket=wayland

modules:
  - name: listenmoe
    buildsystem: simple

    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/listenmoe/cargo
        APP_ID: io.github.noobping.listenmoe

    build-commands:
      - cargo build --release
      - install -Dm0755 target/release/listenmoe /app/bin/listenmoe
      - install -Dm0644 data/scalable/apps/io.github.noobping.listenmoe.svg /app/share/icons/hicolor/scalable/apps/io.github.noobping.listenmoe.svg
      - install -Dm0644 data/io.github.noobping.listenmoe.desktop /app/share/applications/io.github.noobping.listenmoe.desktop
      - install -Dm0644 data/io.github.noobping.listenmoe.metainfo.xml /app/share/metainfo/io.github.noobping.listenmoe.metainfo.xml
      - install -Dm0644 data/io.github.noobping.listenmoe.screenshot.png /app/share/metainfo/io.github.noobping.listenmoe.screenshot.png
      - install -d /app/share/icons/hicolor/symbolic/apps
      - install -m644 data/symbolic/apps/* /app/share/icons/hicolor/symbolic/apps/
      - |
        for f in po/*.po; do
          lang=$(basename "$f" .po)
          mkdir -p "/app/share/locale/$lang/LC_MESSAGES"
          msgfmt "$f" -o "/app/share/locale/$lang/LC_MESSAGES/${FLATPAK_ID}.mo"
        done

    sources:
      - type: git
        url: https://github.com/noobping/listenmoe.git
        tag: v1.0.0
      - type: file
        path: cargo-sources.yml
      - cargo-sources.yml