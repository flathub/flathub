app-id: io.gitlab.zehkira.Monophony
runtime: org.gnome.Platform
runtime-version: '43'
sdk: org.gnome.Sdk
command: monophony
finish-args:
  - --socket=pulseaudio
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --share=network
  - --filesystem=xdg-music/monophony:create
  - --device=dri
modules:
  - python3-modules.json
  - name: monophony
    buildsystem: simple
    build-commands:
      - mv source/* .
      - make prefix=${FLATPAK_DEST} install
    sources:
      - type: git
        url: https://gitlab.com/zehkira/monophony.git
        tag: v1.0.4
        commit: 99698a91a6cb13d0db8f7de8b957165788bec9df
    cleanup-commands:
      - pip3 uninstall -y nuitka ordered-set
