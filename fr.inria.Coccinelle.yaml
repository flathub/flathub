app-id: fr.inria.Coccinelle
runtime: org.freedesktop.Sdk
runtime-version: '23.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.ocaml
command: spatch

finish-args:
  # spatch is a CLI programs that expects file/directory paths as arguments,
  # some of which it will need to write to.
  - '--filesystem=host'
  # For use with `mktemp -d`.
  - '--filesystem=/tmp'

build-options:
  append-path: /usr/lib/sdk/ocaml/bin

modules:
  - name: coccinelle
    buildsystem: autotools
    sources:
      - type: git
        url: https://github.com/coccinelle/coccinelle.git
        tag: '1.2'
        commit: 969cb12e9e9b7d4f42c2ff15296fd927f1ba63af
      - type: file
        path: fr.inria.Coccinelle.metainfo.xml
    post-install:
      - install -Dm644 fr.inria.Coccinelle.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo
