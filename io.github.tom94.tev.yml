id: io.github.tom94.tev
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
command: tev

finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=fallback-x11
  - --device=dri
  - --socket=wayland
  # Full network access to enable the tev's IPC
  # - --share=network

modules:
  - name: tev
    buildsystem: cmake-ninja
    config-opts:
      - -DTEV_DEPLOY=1
      - -DCMAKE_BUILD_TYPE=Release
      - -DTEV_LINUX_REVERSE_DNS_APP_ID=1
    sources:
      - type: git
        url: https://github.com/Tom94/tev.git
        tag: v2.6.0
        commit: 178993f79907bc52db265097fd6b0d78cb1c2e90
        x-checker-data:
          type: git
          tag-pattern: ^v([\d.]+)$
      - type: patch
        path: 0001-Add-release-date-to-MetaInfo.patch
