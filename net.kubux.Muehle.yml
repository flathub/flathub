app-id: net.kubux.Muehle
branch: stable
runtime: org.freedesktop.Platform
runtime-version: '21.08'
sdk: org.freedesktop.Sdk
command: muehle
modules:
  - name: muehle
    buildsystem: simple
    build-commands:
      - tar -xjpvf muehle.tbz -C /
    sources:
      - type: file
        url: http://www.kubux.net/muehle/muehle.tbz
        dest-filename: muehle.tbz
        sha256: f6d78747981113595145a326eded221afda857bfdd3cf92acaa90b313ed26300
        size: 994654
finish-args:
  # X11 + XShm access
  - --share=ipc
  - --socket=x11
  # store two numbers in muehle.rc: AI strength and boardsize in pixels
  - --filesystem=xdg-config:create
