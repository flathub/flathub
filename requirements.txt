# Use the following command to regenerate Python dependencies
# flatpak-pip-generator --runtime=org.gnome.Sdk//50 -o python3-requirements -r requirements.txt --yaml --prefer-wheels pillow

requests
pillow
pycountry
segno
