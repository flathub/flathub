app-id: io.github.nicolettas-muggelbude.myapps
runtime: org.gnome.Platform
runtime-version: '46'
sdk: org.gnome.Sdk
command: myapps

finish-args:
  # Display & Graphics
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri

  # File System Access (benötigt für Paketmanager-Zugriff)
  - --filesystem=host:ro

  # System Information
  - --system-talk-name=org.freedesktop.PackageKit

  # Desktop Integration
  - --share=network

modules:
  # Python Dependencies
  - name: python3-pillow
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} pillow
    sources:
      - type: file
        url: https://files.pythonhosted.org/packages/a5/26/0d95c04c868f6bdb0c447e3ee2de5564411845e36a858cfd63766bc7b563/pillow-10.4.0.tar.gz
        sha256: 166c1cd4d24309b30d61f79f4a9114b7b2313d7450912277855ff5dfd7cd4a06

  # MyApps Application
  - name: myapps
    buildsystem: simple
    build-commands:
      - pip3 install --verbose --exists-action=i --no-index --find-links="file://${PWD}"
        --prefix=${FLATPAK_DEST} --no-build-isolation .
    sources:
      - type: git
        url: https://github.com/nicolettas-muggelbude/myapps.git
        tag: v0.2.0
        commit: ad99594978c0c35b93a9dc97a227decedff96066

      # Metadata
      - type: file
        path: io.github.nicolettas-muggelbude.myapps.desktop
      - type: file
        path: io.github.nicolettas-muggelbude.myapps.metainfo.xml

    post-install:
      # Install desktop file
      - install -Dm644 io.github.nicolettas-muggelbude.myapps.desktop -t /app/share/applications/

      # Install appdata
      - install -Dm644 io.github.nicolettas-muggelbude.myapps.metainfo.xml -t /app/share/metainfo/

      # Install icon
      - install -Dm644 assets/icons/default-app.png /app/share/icons/hicolor/256x256/apps/io.github.nicolettas-muggelbude.myapps.png
