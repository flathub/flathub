app-id: com.sireliah.Dragit
runtime: org.freedesktop.Platform
runtime-version: '20.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --filesystem=home
build-options:
  append-path: "/usr/lib/sdk/rust-stable/bin"
command: dragit
modules:
  - name: dragit
    buildsystem: simple
    cleanup:
      - "/include"
      - "*.a"
    build-commands:
      - install -Dm644 static/logo_icon_t.svg /app/share/icons/hicolor/scalable/apps/com.sireliah.Dragit.svg
      - install -Dm644 dragit.desktop /app/share/applications/com.sireliah.Dragit.desktop
      - install -Dm644 com.sireliah.Dragit.metainfo.xml /app/share/metainfo/com.sireliah.Dragit.metainfo.xml
      - cargo build --release --offline
      - install -Dm755 target/release/dragit /app/bin/dragit
    sources:
      - type: archive
        url: https://github.com/sireliah/dragit/releases/download/v0.5.0/vendored_packages_v0.5.0.tar.gz
        sha256: 15784b6b7fdb3379022b815872938b8773ce2f20159af4b87bab0d14f20e21ae
