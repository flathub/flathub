# Generated from flatpak-flutter.yaml, do not edit
# Visit the flatpak-flutter project at https://github.com/TheAppgineer/flatpak-flutter
app-id: io.github.meypod.al_quran
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm21
command: al_quran
finish-args:
  - --share=ipc
  - --device=dri
  - --socket=x11
  - --socket=wayland
modules:
  - name: al_quran
    buildsystem: simple
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm21/bin:/var/lib/flutter/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm21/lib
      env:
        PUB_CACHE: /run/build/al_quran/.pub-cache
    sources:
      - pubspec-sources.json
      - type: git
        url: https://github.com/meypod/al-quran.git
        commit: 93a79ebdb4b9ad70d7e0b046eefd2130d9550259
    build-commands:
      - setup-flutter.sh -C .
      - flutter build linux --release --no-pub --suppress-analytics
      - install -D $BUNDLE_PATH/al_quran /app/bin/al_quran
      - cp -r $BUNDLE_PATH/lib /app/bin/lib
      - cp -r $BUNDLE_PATH/data /app/bin/data
      - install -D linux/icon.png /app/share/icons/hicolor/256x256/apps/io.github.meypod.al_quran.png
      - install -D linux/io.github.meypod.al_quran.flatpak.desktop /app/share/applications/io.github.meypod.al_quran.desktop
      - install -D linux/io.github.meypod.al_quran.metainfo.xml /app/share/metainfo/io.github.meypod.al_quran.metainfo.xml
    modules:
      - flutter-sdk-3.38.1.json
