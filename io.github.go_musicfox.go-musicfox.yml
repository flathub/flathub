id: io.github.go_musicfox.go-musicfox
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
    - org.freedesktop.Sdk.Extension.golang
command: go-musicfox

finish-args:
    - --share=network
    - --socket=pulseaudio
    - --talk-name=org.freedesktop.Notifications

build-options:
  env:
    - GOBIN=/app/bin
    - GOROOT=/usr/lib/sdk/golang

modules:
  - name: go-musicfox
    buildsystem: simple
    build-commands:
      - $GOROOT/bin/go build -trimpath -o go-musicfox cmd/musicfox.go
      - install -Dm00755 go-musicfox ${FLATPAK_DEST}/bin/go-musicfox
      # Upstream icon is 1024x1024. Shrink to 512x512 to conform to icon standard. 
      - ffmpeg -hide_banner -i previews/logo.png -vf scale=512:512 app-icon.png
      - install -Dm00644 app-icon.png ${FLATPAK_DEST}/share/icons/hicolor/512x512/apps/${FLATPAK_ID}.png
      - install -Dm00644 io.github.go_musicfox.go-musicfox.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm00644 io.github.go_musicfox.go-musicfox.appdata.xml -t ${FLATPAK_DEST}/share/metainfo
    sources:
      - type: git
        url: "https://github.com/go-musicfox/go-musicfox.git"
        tag: v4.5.7
        commit: 1629dd8dbaa2f433bd9011db58ba3c639a15fd0c
      - type: file
        path: io.github.go_musicfox.go-musicfox.appdata.xml
      - type: file
        path: io.github.go_musicfox.go-musicfox.desktop
