app-id: one.ablaze.floorp
name: Floorp
runtime: org.freedesktop.Platform
runtime-version: 22.08
sdk: org.freedesktop.Sdk
add-extensions:
  org.freedesktop.Platform.ffmpeg-full:
    directory: lib/ffmpeg
    version: 22.08
    add-ld-path: "."

command: floorp

finish-args:
  - --share=ipc
  - --socket=pcsc
  - --socket=x11
  - --socket=wayland
  - --share=network
  - --socket=pulseaudio
  - --persist=.floorp
  - --filesystem=xdg-download
  - --filesystem=xdg-documents
  - --filesystem=xdg-desktop
  - --filesystem=xdg-music
  - --filesystem=xdg-pictures
  - --filesystem=xdg-videos
  - --device=all
  - --talk-name=org.freedesktop.FileManager1
  - --talk-name=org.a11y.Bus
  - --talk-name=org.gnome.SessionManager
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.freedesktop.ScreenSaver
  - --system-talk-name=org.freedesktop.NetworkManager
  - --own-name=org.mozilla.floorp.*

modules:
  - name: floorp
    buildsystem: simple
    build-commands:
      - install -Dm644 one.ablaze.floorp.appdata.xml -t /app/share/metainfo/
      - mv ${FLATPAK_BUILDER_BUILDDIR}/*.tar.xz ${FLATPAK_BUILDER_BUILDDIR}/floorp.tar.xz
      - cd /app/ && tar Jxvf ${FLATPAK_BUILDER_BUILDDIR}/floorp.tar.xz
    sources:
      - type: file
        url: 'https://sda1.net/storage/floorp/tarball/10.9.0/floorp.tar.xz'
        sha256: 3e81c1e4a29dafc939872544528bfc36334e68b1cfaefa9f652a677bb028b732
      - type: file
        path: one.ablaze.floorp.appdata.xml


