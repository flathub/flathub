id: page.codeberg.jaerrib.wightwatch_keep
runtime: org.freedesktop.Platform
runtime-version: '25.08'
base: org.godotengine.godot.BaseApp
base-version: '4.7'
sdk: org.freedesktop.Sdk
command: godot-runner

finish-args:
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --socket=pulseaudio
  - --device=dri

modules:
  - name: wightwatchkeep
    buildsystem: simple

    sources:
      - type: git
        url: https://codeberg.org/jaerrib/wightwatch_keep.git
        commit: d66e786412a6171fa2923e3506713e24ad050719

      - type: file
        url: https://codeberg.org/jaerrib/wightwatch_keep/releases/download/1.5.2/Wightwatch_Keep-1-5-2.pck
        dest-filename: Wightwatch_Keep.pck
        sha256: 9c150e1d4320719b2f3daee311661d417d3518a4ce860f5a05790791268cddbb

    build-commands:
      - install -Dm644 Wightwatch_Keep.pck ${FLATPAK_DEST}/bin/godot-runner.pck
      - install -Dm644 ${FLATPAK_ID}.desktop ${FLATPAK_DEST}/share/applications/${FLATPAK_ID}.desktop
      - install -Dm644 ${FLATPAK_ID}.xml ${FLATPAK_DEST}/share/metainfo/${FLATPAK_ID}.metainfo.xml
      - install -Dm644 ${FLATPAK_ID}.png ${FLATPAK_DEST}/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
