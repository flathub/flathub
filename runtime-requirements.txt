# System dependencies:
# macOS: 
#   brew install hunspell
#   brew install hunspell-en
# Ubuntu/Debian: 
#   sudo apt-get install hunspell hunspell-en-us hunspell-en-gb
# Fedora: 
#   sudo dnf install hunspell hunspell-en

pyenchant>=3.2.0
feedparser>=6.0.0
requests==2.31.0
