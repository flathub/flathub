# Contracts

Stable implementation contracts that downstream work can build against without
reverse-engineering Rust modules. Add or update a contract here whenever a phase
freezes a backend-facing interface for the UI agent or future contributors.
